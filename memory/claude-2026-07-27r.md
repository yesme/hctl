# Claude Memory 2026-07-27r — 主笔安排：seat init 阶段拆解 + head 前进重审治理

> 性质：**主笔安排**（用户 2026-07-27 交办：「grok 讨论中两件事需主笔安排——hctl init 的阶段、head 移动导致反复 gate」）。输入：grok-27j（PR #41，seat init + trailer 机械化）、grok-27l（PR #47，重审浪费谱系）、codex-27h/27i（#38 两轮 RC 实证）、27o F4 / 27q 时序实操。
> 会话实况：Claude Code / claude-fable-5 / max effort。
> 权限边界：流程层安排以 coordinator 身份即日生效；涉 kernel/法条者为排班提案，assignment 创建候用户授权（D-38 级④：ASSIGN 确由人发起）。

---

## A. seat init：命名裁定 + 阶段拆解

**采纳 grok-27j 全部问题定性**（归因靠级④ prose 已两度出事：claude 席 27 日前全部 commit 错 bot 邮箱；grok #38 P1-05 至今仍卡 re-gate）。安排如下：

### A1. 命名裁定

- 叶子命令定名 **`hctl seat init`**（采 grok 备选）：seat 名下动词，为将来 seat 域叶子命令留名字空间。
- **裸 `hctl init` 本阶段不设**：repo 脚手架（初始化 .hctl/、seats 骨架）是 adopters 面需求，P3 与 abacistopia 影子 round 同车再议；避免与 `git init` 心智冲突在最贵的窗口发生。corpus 的 `hctl_fixture.sh` 继续手工搭沙盘（D-17 显式性，不依赖被测物自举）。

### A2. 阶段拆解

**P1.1「归因收口」批（建议 #38 收口后即开）——范围四件，皆小：**

1. **kernel loader 先行**认识 seats 可选字段 `coauthor_email`（config loader 是闭集解码——字段先入 kernel、后入 seats.toml，两步走，同 codex-27f E1 的反循环纪律）。
2. **seats.toml 补三席 `coauthor_email`**（gated `.hctl/**` 变更，走 D-38 required gate）；表值取 grok-27j §3.1 abacistopia 实测表（grok=302482056、claude=281844019、codex=281847692 各自 bot）。
3. **`hctl seat init` 最小版**：写 `.git/hctl/local.toml`（seat 确认自 worktree 后缀/branch pattern/`--seat`、coauthor_email default←seats.toml 可 override、machine 别名）；idempotent；`doctor` 检缺失与 seat↔email 错配。
4. **`hctl trailer` 消费链收口**：email←local∪seats；model/effort 仍←会话实况 env（现状源）；**任一不可得 exit≠0**（D-23 既有基因，只是把 email 源从人脑换成配置链）。

**P2（与 GLM/Kimi/AGY 入仓同车）：**

5. per-harness **session 证据 adapter**（grok `summary.json`、claude/codex 各家证据源——仿 abacistopia `coauthor.py`），把 model/effort 的源从 env 升为 session 文件；adapter 逐家实测后补，与 adopters onboarding 天然同批。
6. `commit-msg` hook / `hctl commit` 薄壳（诚实标注：hook=级③检测，非级②证明——D-17 降级诚实条款）。
7. bot 邮箱表补 GLM/Kimi/AGY 行。

### A3. 排班提案（候用户授权）

- assignment **`p1-attribution`**：author=**grok**（提案者，携 coauthor.py 语境），gates=codex precision + claude spec-conformance，squash。开工前置：#38 收口。
- **过渡纪律**：工具未落地前 trailer 仍是级④手抄——**#38 的 P1-05 按 codex 要求 amend 收口，不等工具**；各席以 memory 规则自警（claude 席已入长期记忆）。

---

## B. head/base 前进 → 反复 gate：治理安排

**采纳 grok-27l 的两层定性**：freshness=OID 字面全等是安全核的定价，不是 bug；松弛只允许落在 **quorum 计票侧的机械可证继承谓词**，claim 阻尼与 fencing（D-34/D-35）一寸不动。grok §4 的「明确不做」表（纯时间窗、latest 自动继承、LLM 眼缘判等价、head 动 claim 不 stale）照单全收为红线。

### B1. 即刻生效（级④ 流程层，coordinator 权限，本 memo 即载体）

1. **批量 memo**：required gate round 进行中，verdict/re-nail memo **只开 PR 不合 main**；coordinator 在静默窗口一次批合全部 quorum memo → author 单次 rebase → 各 gate tree-identity 一行 re-nail → 随即 squash。base 前进从每轮 N 次收敛为 1 次。（D-40「report 可达于 base」是 quorum **受理时**前置，批合在受理前完成即满足——不破硬门。）
2. **作者 freeze tip**：gate 开跑后 author 不 push；修复轮例外（修复=新 revision，本就重审）。
3. **文案纪律**：status/brief/开工文书禁暗示「任何 OID 变 ⇒ 必须 full」——第二轮默认形状是 D-41 composite（fix_verification + delta），27q/codex-27i 均已实践。
4. **#38 终局即照此走**：grok 修 codex-27i 三条 → 我批合届时全部在途 memo → grok 最后一次 rebase → 两席一行 re-nail → 静默窗口 squash。

### B2. 第一刀内核松弛：tree-equivalent carry（排班提案）

采纳 grok §3.2 为唯一第一刀，主笔加严为**四条件全满足才继承**：

```text
inherit(V{base,head} → {base',head'}) ⇔
    tree(head) == tree(head')                       # 内容等价（空 rebase/改 message）
  ∧ is-ancestor(base, base')                        # base 只前进，无回卷
  ∧ tree_at(base', .hctl/) == tree_at(base, .hctl/) # 治理面等价（assignment 移动在 obligation
                                                    #   identity 上已结构自失效；此条显式挡
                                                    #   seats.toml rebind/enforcement 类变更）
  ∧ V.decision==APPROVE ∧ V.completeness==COMPLETE  # 只继承绿票；RC 不继承
```

- 落点：derive/quorum 继承谓词（标 `carried: tree-equivalent` 入计票解释）+ **语料两支**（base 前进 tree 同 → carry 计入 green；tree 异或 `.hctl/` 动 → 不 carry）+ amends 台账 **D-04/D-40**（计票例外）、D-41 注记（继承后 scope 语义）。法条 amendment 文本**随实现 PR 同批落 DECISIONS**——法随强制走，不提前空转。
- 排班提案：assignment **`p1-regate-carry`**：author=**codex**（kernel Go，D-24 先例），gates=grok adversarial + claude spec-conformance，squash。与 `p1-attribution` 不同 author，可并行（27j 双 lane 先例）。
- 显式让继承是**计票层显像**（status 可见 carried 标记），非静默——审计面不失。

### B3. 后续阶梯（登记不开工）

- **P2**：`review_surface` path-disjoint carry——前置是补 D-41 E3 的 surface map 裁定（27o 已记该保守留白），单独立项，勿混入第一刀。
- **P3**：freeze ref / 两阶段评审 + D-37 严格 base 旋钮再议（D-37 留口原文）。

### B4. 效果核算（以本轮 #38 为标本）

流程层批量 memo 可省本轮已发生的 4 次 base 追赶（27p→#42→#44→27q→#48 逐笔推进 main）；tree-carry 再省终局 2 次一行 re-nail。**大头在流程层**——先行零协议改动即拿走多数浪费，内核松弛拿走残余的机械成本。

---

## 待用户裁

1. `p1-attribution`（grok author）与 `p1-regate-carry`（codex author）两 assignment 是否授权、先后或并行。
2. B1 流程层无需裁（coordinator 域内），已即日生效。

## Commits 本次会话

- 本 memory（memo lane 微 PR）。
