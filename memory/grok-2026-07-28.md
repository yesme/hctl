# Grok Memory 2026-07-28 — VERDICT：p1-bootstrap-cutover adversarial re-gate（scoped mutation-sensitivity）

> 性质：required gate **re-review**。assignment=`p1-bootstrap-cutover`，gate=`adversarial`。
> 范围（唯一 ledger）：**仅** codex-pr72#P1-01..03 的 mutation-sensitivity 对抗面 + delta（`10d333e→a79d0af`）；**不重开 full**。
> 输入：reconciled packet on #72；author T_final `a79d0af`；27t 同 base 的 full APPROVE 记 conflicting evidence 不抹 findings。
> 会话：Grok 4.5 / high effort。日期取系统时钟 2026-07-28。

## 0. Frozen revision（T_final）

```text
PR:        #72
base:      9c20adfcbc593cb905daf638a558d36d81917772
input:     10d333e1d62b98d05f906027fe9e1a264d06a33f   # full-gate head (27t)
T_final:   a79d0af38d8055e5d354408add504a8ce17cd4de
tree:      b659b323b0c64b0acc6f393ec5a3f70145c8cb11
parent(T): 10d333e (== input)
scope:     composite{
             fix_verification(P1-01..03, target_tree=b659b323…),
             delta(from=0824619d…@10d333e, to=b659b323…@a79d0af)
           }
completeness: COMPLETE
decision: APPROVE
```

## 1. Preflight

- 单修复 commit 于 freeze tip 之上；delta 7 paths（cutover/merge_history/tests/corpus#30/D-38/METHOD）。
- assignment blob 未动。trailer：Fable 5 max + claude bot ✓。
- 机械（detached T_final）：derive 相关单测 PASS；`HCTL=<built>` strict corpus **32/32 skip=0**；case #30（含 §3.5）PASS。

## 2. Mutation-sensitivity 表（ledger 三闭包）

### codex-pr72#P1-01 — unknown enforcement 永不成边界

| 列 | 证据 |
|---|---|
| **原反例** | 不可读/类型错/坏 TOML 折叠为 non-active → 删 mid-active blob 推 cutover 向 tip 扩 ACK；删 root-active 可藏多翻转 |
| **相邻 mutation** | 三态：`active` / 可解码 non-active / path 确缺=non-active；garbage/未知 → `INVALID_CUTOVER` 且 `cut==nil` |
| **mutation sensitivity** | `TestDetectCutoverUnknownObjectFailsClosed`：A 删 mid-active blob 必 INVALID 无边界；B 删 root-active 于 active→bootstrap→active 必仍 INVALID。`garbage` 由旧 happy 改判 INVALID。拆三态回折叠 ⇒ 两 mutation 必绿变红 |
| **happy** | `none→bootstrap→active` 仍得 cutover；path 缺≠unknown |

对抗复核：`enforcementStateAt` 仅在 `ls-tree` 空 listing 时 non-active；blob 不可读且 path 仍列在 tree → unknown → 拒边界。**无法**用缺对象伪造「更浅 cutover」。

### codex-pr72#P1-02 — tree 相等永不单独放置债

| 列 | 证据 |
|---|---|
| **原反例** | tip-first 全史 tree 匹配可把 post-cutover 候选钉到 cutover OID → 误 ACK |
| **相邻 mutation** | `candidateAnchor`（head 沿 first-parent 至 main 可达点）+ `IsAncestor(anchor, integration)`；无绑定 squash 且 head reachable → `UNJUDGEABLE_MERGE` error（`mergedOID=""` 不 covers） |
| **mutation sensitivity** | corpus #30 §3.5：side candidate tree=`cutover^{tree}`、双亲 integration → 断言 `UNJUDGEABLE_MERGE error` 且 **detail 不含 demo3 的 ACK**。拆 ancestry guard 回纯 tree match ⇒ 该段必失败（会继承认账） |
| **happy** | 段 1–3 真 unrecorded / post-cutover unrecorded 路径保留 |

对抗复核：anchor 在 post-cutover tip 时，cutover OID 是 tip 的祖先而非子孙，`IsAncestor(tip, cutover)` 假 → 不放置。27t **N1**（tree-placement）由本闭包 **supersede 为 CLOSED 面**。

### codex-pr72#P1-03 — allowlist == {UNRECORDED_MERGE}

| 列 | 证据 |
|---|---|
| **原反例** | covered 债一律降 warning → INVALID_RECEIPT/UNJUDGEABLE 释放 WriteGuard |
| **相邻 mutation** | `markMergeDebt`：仅 `code=="UNRECORDED_MERGE" && covers(oid)` → ACK warning；其余保原 code+error |
| **mutation sensitivity** | `TestMarkMergeDebtAllowlist` 九行：三类 × {inside,outside,unplaced}；仅 inside×UNRECORDED 绿 WriteGuard。拆 allowlist 或扩大 covers 语义 ⇒ 表驱动必红 |
| **法条** | D-38 / METHOD §10 撤回 prose 自扩，钉 allowlist 恰一类 + 三态 + 祖先绑定 |

## 3. Delta 面（adversarial，非 owner CLOSED）

```text
from_tree @10d333e: 0824619d041c64582a88e14500b5740a54d66d44
to_tree   @a79d0af: b659b323b0c64b0acc6f393ec5a3f70145c8cb11
```

- 无 assignment 自改；无 scope 外内核面。
- 行为差：unknown 拒边界、tree-reuse 拒 ACK、allowlist 收窄——均朝 fail-closed，未发现新的 false-green 旁路。
- **N1（hygiene，不阻断）**：`detectCutover` 函数头注释仍残留「undecodable=not-active」旧叙述，与实现三态不一致；建议 author/hygiene 随笔，不升 finding。

## 4. Disposition

```yaml
decision: APPROVE
completeness: COMPLETE
scope: composite{fix_verification(P1-01..03), delta(10d333e→a79d0af)}
findings: []
notes: [N1 stale detectCutover header comment]
# ownership: 仅 codex 可将 P1-01..03 置 CLOSED；本席 supporting evidence
```

候 codex owner fix_verification 同 T_final；双绿后批合 gate memo（#73/#74/本 memo）→ 静默窗口 squash。
