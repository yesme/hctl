# Claude Memory 2026-07-27m — D-24 范围澄清：内核 Go，语料设施豁免（差分 oracle 论）

> 性质：用户问答定案。问「codex 还没写完。现在 grok 还需要把 python 改成 go 么？」→ 判**不需要**；裁决「准。D-24 范围澄清入台账。」（2026-07-27）。
> 会话实况：Claude Code / claude-fable-5 / max effort。基线：main @ `0c6dcdc`。

## 定案

1. **D-24 约束的是内核交付物**（分发到每台机器的二进制）——漂移/依赖论据长在「分发」上；语料/测试设施不分发、只在开发机跑，允许 **bash + python3-stdlib**（json/hashlib 级，无 tomllib 类版本坑）。
2. **lib 纯谓词（jcs.py / obligation.py / derive_rules.py）定位为差分 oracle**：独立于内核的第二实现，Go JCS 与 Python JCS 对 corpus #28 向量必须吻合——独立实现互证强于 Go 测 Go。纪律：**normative authority 永远是 Go 内核**；oracle 与内核不一致 = 红旗调查，不自动裁谁赢。
3. **集成时刻（kernel 落 main 后）的一次性接线**：hctl-wire 案 SKIP(77)→真断言；derive_rules.py 中与内核重复的裁决逻辑二选一——退役（期望值冻成字面量入案文）或明确降级为差分 oracle。作为 claude spec-conformance verdict 的条件下达，由 grok 按 finding 处理，非即时返工。

## 背景评估（修正 27l §4 的预判）

grok 重写的 corpus README 未踩 stale 文档坑：旧「Python runner 收编（K-24）」线已被其自行清除；四模式分类（pure / hybrid / deferred-p2 / hctl-wire + `HCTL=` 接线钩子 + SKIP 不记 fail）把与 p1-kernel 的并行接线协议写得干净，且自定 quorum green 条件=「runner 全绿且接线后仍绿」。主笔在 27l 预登记的 stale-doc 债实际已被 author 消化，记撤。

## Commits 本次会话

- 本 memory + DECISIONS D-24 范围澄清（同批微 PR）。
