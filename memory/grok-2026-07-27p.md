# Grok Memory 2026-07-27p — VERDICT：p1-regate-carry adversarial gate（PR #62）

> 性质：**required gate verdict**（assignment `p1-regate-carry` / gate id `adversarial` / seat `grok`）。
> 对象：PR https://github.com/yesme/hctl/pull/62 · 分支 `work/codex/p1-regate-carry`。
> 会话：Grok 4.5 (high effort) / 对抗镜头。
> 输入：PR body frozen packet + head 树（`internal/derive/carry.go` 等）+ D-04/D-35/D-40/D-41 + METHOD §8.1 + codex-27k §3.1。
> 验证：detached scratch @ exact head；go test/race/vet；strict corpus **31/31**（含 #29）；#29 亲跑 PASS。

---

## 0. Verdict（D-41 / D-40）

```yaml
gate_id: adversarial
gater_seat: grok
assignment: p1-regate-carry
decision: APPROVE
completeness: COMPLETE
threshold: P1
scope:
  kind: full
revision:
  base: "44e871711f62d4620a6675436ea4f0cb73ce4a30"
  head: "7bed0cae2cf65833d51856f94100f94f2337cd25"
  tree: "eca30fea20365a3ff3e249532bb7d1d58127adc8"
report: memory/grok-2026-07-27p.md
late_findings: []
```

**总判：APPROVE + COMPLETE。**
memo-base-equivalent carry 的安全边界在对抗面成立：正例可达 merge guard；whitespace / message / non-memo base / 多类 unit 反例 fail-closed。零 P0/P1 阻断。

**exact revision 核验：**

- `HEAD` = `7bed0cae2cf65833d51856f94100f94f2337cd25`
- `HEAD^{tree}` = `eca30fea20365a3ff3e249532bb7d1d58127adc8`
- `merge-base --is-ancestor 44e87171… HEAD` 成立
- 单 commit；Co-authored-by Codex bot

---

## 1. 对抗审查范围

| 攻击面 | 结论 |
|---|---|
| **patch-id 假绿** | 代码路径用 exact blob/mode/type OID delta + full message；unit+wire 有 whitespace 反例；禁用 patch-id 写进 D-40 |
| **author/message framing** | fingerprint 含 author name/email bytes 与完整 message；unit：author-identity、commit-message |
| **binary/mode/object identity** | tree side 编码 mode/type/oid；unit：file-mode；内容变 ⇒ blob OID 变 |
| **first-parent 线性** | multi-parent candidate 直接拒；unit：non-linear-candidate |
| **non-memo base** | `ValidMemoPath` + path.Clean 防 `..`；unit：source-base-advance；wire：README.md base 前进不 carry |
| **candidate-path ∩ base-delta** | `oldCandidate.paths[change.path]` 拒；逻辑在 `memoBaseEquivalent`（unit 未单独命名，但与 memo path 集合检查同构） |
| **historical PR / PR 变** | `v.PR != state.PR` 拒；unit：different-pr |
| **latest-verdict** | 有更新 REQUEST_CHANGES 不 carry；unit：newer-verdict |
| **REQUEST_CHANGES / INCOMPLETE** | 明确拒；unit 覆盖 |
| **claim fencing** | 要求 `state.Claim.OID == v.Claim` 且非 escalated；carry 不改写 claim、不 cast 新事件；stale claim 可 carry（法律一致） |
| **missing objects** | `CommitKnown` / batch read 失败 ⇒ false |
| **merge-quorum 传播** | unit 正例 merge claimable green；wire #29 `merge --check` 过 Hctl-Head |
| **status 证据呈现** | JSON `carried` + 人读 original verdict/revision/report |

---

## 2. 攻击结果

### P0 — 无

### P1 — 无阻断

实现与 codex-27k §3.1 谓词一致：

```text
carry iff APPROVE+COMPLETE+covering
  ∧ same assignment/gate obligation/branch/PR
  ∧ claim == verdict.claim (stale OK, escalated 否)
  ∧ no newer overall verdict
  ∧ is-ancestor(old.base, new.base)
  ∧ base delta ⊆ memory/** ∧ disjoint candidate paths
  ∧ candidate_fp equal (single-parent only)
```

假绿路径（whitespace 等同 patch-id、改 message、源码 base 前进、改 PR、改 decision）在 unit 与/或 wire 上均被拒。

### P2 — 残留（不阻塞）

1. **candidate-path ∩ memo-base 无独立命名 unit**
   逻辑已实现；建议后续加 fixture：candidate 改 `memory/x`，base 再改同一 path，断言不 carry。非本 PR 假绿证据。
2. **ListTree 扁平 -r**
   正确；子目录 blob 以完整 path 入 fingerprint。
3. **protocol 文案级规则（ownership / ESCALATED budget）**
   已上迁 DECISIONS/METHOD；机器 ledger 仍 P2（27k 明确）——对抗不要求本 PR 实装 finding ledger。

---

## 3. 机械 transcript

```text
revision:
  base 44e871711f62d4620a6675436ea4f0cb73ce4a30
  head 7bed0cae2cf65833d51856f94100f94f2337cd25
  tree eca30fea20365a3ff3e249532bb7d1d58127adc8

GOPROXY=off go test -mod=vendor ./...     PASS
GOPROXY=off go test -race derive/app      PASS
GOPROXY=off go vet -mod=vendor ./...      PASS
git diff --check base..head               PASS
HCTL=… CORPUS_REQUIRE_HCTL=1 run.sh       pass=31 fail=0 skip=0
HCTL=… cases/29-regate-carry.sh           PASS
```

---

## 4. 处置

- **decision=APPROVE** @ frozen head 上。
- 候 **claude** `spec-conformance` 对同一 head。
- 双绿后：batch gate memos → 静默窗口 squash（本 carry 即为此类 base 前进场景设计）。
- 本席不要求 author 再 push。

## Commits 本次会话

- 本 memory（memo lane 微 PR）。
