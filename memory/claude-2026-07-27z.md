# Claude Memory 2026-07-27z — VERDICT：p1-attribution spec-conformance @ T_final（含 27y 更正）

> 性质：**gate verdict**（reconciled-repair-1 轮，取代 27y 作为本席现行判定）。obligation：p1-attribution / gate / {spec-conformance, claude}。ledger：PR #65 评论（coordinator 归一版）。
> 会话实况：Claude Code / claude-fable-5 / max effort。

```yaml
scope:
  kind: composite
  parts:
    - kind: full
      target: "PR #65 T_final 64a4b5f8b20a8e41b7ffd032e20e77bb5265283b（单 commit 重铸于 base，tree 432a9eb）"
    - kind: fix_verification
      findings: [claude-27y#correction]
      target_blob: "432a9eb97d20a79cab50e75202784125dc564054"
decision: APPROVE
completeness: COMPLETE
late_findings: []
revision:
  base: "f98255dc61be6e3f4be88fa36802334fcfbe5442"
  head: "64a4b5f8b20a8e41b7ffd032e20e77bb5265283b"
not_evaluated:
  - {id: codex-pr65#P1-01, owner: codex}
  - {id: codex-pr65#P1-02, owner: codex}
  - {id: codex-pr65#P1-03, owner: codex}
  - {id: codex-pr65#P1-04, owner: codex}
```

## 1. 27y 更正（自 ledger conflicts-resolved 落账）

27y 在旧 head 上 APPROVE 有三处不当，本篇正式更正：**P1-02 误判**（表扬 CommonDir「worktree 安全」——linked worktree 恰共享 CommonDir，方向全反）；**P1-01/P1-04 漏检**（未审 assignment diff、未测离线）；**N2 过宽**（env 逃生门当时未过归属校验）。教训归档：spec 面必须含 `.hctl/**` diff 与离线路径两个必查项，已入本席检查单。

## 2. spec 面判定（T_final）

- **形状**：单 commit 重铸于 base（push budget=1 ✓）；trailer `Grok 4.5 high` + grok bot ✓；**assignment 文件对 base 字面相同**（`git diff base..head -- .hctl/assignments/` 空）；`.hctl/` 净变更仅 seats.toml 三行邮箱表（交付物本体，D-37 config 域）。
- **四修复的 spec 面**（closure 归 codex，此处为 supporting evidence）：`LocalConfigPath=repo.GitDir`（per-worktree，注释即 `--git-path` 等价）；`RejectEmailOwnedByOtherSeat` 单一守卫定义于 identity、seat init 写路径与 Resolve 双挂、**local 错配先于 env 判**（env 无法绕过）；`loadAttributionLocal` 走 `config.LoadAt(HEAD)` 纯本地读、零 ref-plane observer。
- **测试证据与 ledger 闭合表一一对应**：LinkedWorktreeLocalAttributionIsolation / TrailerDoctorSamePayloadNoForkOnCrossSeatEmail / AttributionOfflineWhenOriginUnreachable / LocalConfigPathIsGitDirNotCommonDir / ResolvePrecedenceAndOwnership。identity 包首获单测（27o F6 旧注记顺带减账）。
- **机械亲跑**：vet ✓、`go test ./internal/...` 全包 ok、strict corpus 31/31 零 skip。
- 27y N1（trailer 规范形加冕）仍为用户裁量 note，不阻断。

## 3. 处置

decision=**APPROVE + COMPLETE @ {f98255d, 64a4b5f}**。quorum 候 codex owner fix_verification(P1-01..04)；双绿后批合 gate memo（#66/#67/本篇/codex 新篇）→ 静默窗口 squash。本 memo PR 开而不合。
