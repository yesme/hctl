# Grok Memory 2026-07-27h — p1-corpus fix-forward：wire + strict runner + oracle 收口

> 性质：author 实装 memo（非 gate verdict）。assignment 心智：`p1-corpus` post-merge fix-forward（销 codex-27g P1-01..04 + claude-27n F1）。
> 会话：Grok 4.5 / grok-build / Ubuntu worktree `hctl-grok`。
> 分支：`work/grok/p1-corpus-wire`（from `origin/main` @ `09d1edc`，含 p1-kernel）。
> 输入：`memory/codex-2026-07-27g.md`、`memory/claude-2026-07-27n.md`、METHOD D-17/D-24/D-33/D-37/D-39/D-41。

## 0. 席位/分支自正

- 弃 `work/grok/build-kernel` 占位（tip=main、非任务 slug）。
- 新任务分支 `work/grok/p1-corpus-wire` 基于 **当前 main**（非旧 PR #25 head `3efba42`）。
- 双机（Mac/Ubuntu）可同名 checkout；只避免双写同一远端 ref。

## 1. 销账对照

| Finding | 处置 |
|---|---|
| **codex P1-01** runner 缺 manifest / SKIP 仍绿 | `run.sh` 冻结 concurrency×2 + cases #01–28；缺案/多案 FAIL；`CORPUS_STRICT=1` 或 `CORPUS_REQUIRE_HCTL=1` 下 SKIP=FAIL |
| **codex P1-02** HCTL 空钩子 / stub 自测 | 真 wire：#3/#8/#9/#11/#12（`hctl_fixture.sh` + claim/begin/status/doctor）；#7/#28 拒 stub binary（help/version + Go package tests）；去掉 #8/#9/#12 等 ad-hoc stub 当 PASS 的段 |
| **codex P1-03** oracle fail-open | `parse_i_json` 拒 surrogate；`three_way_event` 完整信封才分 UNSUPPORTED；`assign_event` exact keys；#28 恢复 lone-surrogate 向量；quorum 补 exact revision 第一条件 |
| **codex P1-04** #7 假 receipt | 全长 OID + `receipt.py` 闭集 parse；截断/未知字段/短 obligation 负例；parent==Hctl-Base；Go `./internal/receipt` 作权威校验 |
| **claude F1** stub 虚增绿 | wire 案接真 CLI；其余 pure 改走共享 `derive_rules` 谓词（差分 oracle），案头标明非 kernel PASS 独据 |
| P2-01/02 | 未本批强修（patterns 假阳、gen_cases drift）— 登记后续 |

## 2. 验证

```text
bash tests/corpus/run.sh
  pass=27 fail=0 skip=3   # wire 案无 HCTL 时 SKIP，非 strict 合法

HCTL=/tmp/hctl CORPUS_REQUIRE_HCTL=1 bash tests/corpus/run.sh
  pass=30 fail=0 skip=0

HCTL=/usr/bin/true CORPUS_REQUIRE_HCTL=1 bash tests/corpus/run.sh
  fail>0   # 空钩子不再全绿

# 移走 cases/28 → manifest FAIL exit 1
```

## 3. 未做 / 下一棒

- 未 push / 未开 PR（等人点火或本席续推）。
- p1-corpus 的 `UNRECORDED_MERGE` 审计债仍在 main；本 fix 合入后须 **带 receipt 的 hctl merge** 或人裁补记路径另议。
- gates：codex precision + claude spec-conformance 对 **本分支** 做 fix_verification+delta（见 codex-27g §3）。
- P2 oracle patterns_disjoint 假阳、gen_cases round-trip 未动。

## Commits 本次会话

- 本 memory + corpus fix-forward 全量（同批）。
