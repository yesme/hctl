# Claude Memory 2026-07-27o — VERDICT：p1-kernel spec-conformance gate

> 性质：**gate verdict**。obligation：p1-kernel / gate / aspect {gate_id: spec-conformance, gater_seat: claude}（`.hctl/assignments/p1-kernel.toml`）。
> 会话实况：Claude Code / claude-fable-5 / max effort。

```yaml
scope:
  kind: full
  target: "PR #33 head d91f38d93b9a1c59521b9b16df157f3cfffdbe7b（分叉点 85643c6…；bootstrap 期 memo-verdict，chain 机制未活）"
decision: APPROVE
completeness: COMPLETE
late_findings: []
```

**评审方法**：scratch worktree（D-14 零常驻 checkout）。全文精读：store（CAS/lease/pending 恢复）、derive.go + merge_history.go（状态机/quorum/receipt 重放）、receipt、buildcheck、app/claim.go、app/merge.go；定向不变量验证（grep+抽读）：config（gater≠author loader 级拒绝 types.go:369、pattern overlap、LoadAt、quorum 校验）、facts（链结构单 parent+单文件 event.json、ls-remote advertised、coop tracking 无 force、first-parent batch 读）、protocol（exactObject=closed-set 解码、Structural/Unsupported 双错类、ObligationID 重算、ValidMemoPath）、app 其余命令面；机械信号：`go build` ✓、8 包单测全绿、二进制 help 自描述 ✓、corpus 30/30（含 `HCTL=` 接线，注：wire 面尚窄，见 27n F1）。非 vendor 6,757 行（去测试）。

## 1. 符合面（判定依据）

- **D-31/D-32/D-34 事件层与 CLAIM**：写路径 blob→单文件树→commit-tree→本地 CAS→lease push（创建=空 expect）；publishing=pending journal，五中间态全实现（ancestry delivered+ff / pending-ahead 重推原 commit / lost 先存 `refs/hctl/recovery/<seat>/<oid>` 再对齐 / tracking≠advertised=INCOMPLETE_FACTS / 远端消失=CORRUPT_CHAIN）；append 前强制 recover-clean（第二 writer 只许恢复）；tracking 只经 fetch。
- **D-34 阻尼确定函数**：streak/escalated 逐事件重放；re-claim 必须 fence active、escalated 拒 re-claim、overdue 声明早于时限=invalid（连 reclaim_reason 的时间合法性都验）；progress 分类含 environment-reset 与 rewrite-or-rollback，锚点 ancestry 证明，缺对象=INCOMPLETE 而非猜。
- **D-40/D-41 verdict 受理**：11 项条件清单（fencing token、exact revision、PR 钉 head、report 可达于 base=「先合 memo」硬门、actor=holder、escalated/stale 拒……）；latest-wins per exact revision；quorum green=Valid+Covers+APPROVE+COMPLETE——**kernel 实现了 corpus oracle 缺的第一条件（exact revision）**。
- **D-37 merge 全链**：claim 前置（green+coordinator+capacity=1+config 钉）；merge 前置再查（严格 base==current main、PR head exact、quorum、config 未动、enforcement 下 .hctl 变更须 required gate）；receipt 闭集字段+dup/未知字段拒；`gh pr merge --squash --match-head-commit --body-file`；post-verify=main 前进+VerifyCommit（单 parent==Hctl-Base+**squash 树==head 树**）+receipt 重放闭合，失败=MERGE_INTEGRITY_INCIDENT。**receipt 重放是完整实现**：LoadAt(Hctl-Base) 双时点、fact-tips 须为现链 prefix、临时快照重推导证明 active claim==Hctl-Merge-Claim+exact-revision green——无时钟重放坐实。
- **D-24 buildcheck**：bootstrap pin 容缺 revision（级④窗口）；SHA pin 下缺 revision/modified worktree=STALE_BINARY fail-closed；**enforcement=active 后 bootstrap pin 非法**（grok 逸出触点完整闭合）；语义版本双轨。
- **D-39 三分法**：protocol 层 Structural/Unsupported 双错类；facts 层链结构校验；derive WriteGuard fail-closed；UNRECORDED/INVALID_RECEIPT/UNJUDGEABLE 区分并入 AuditDebt 态。
- 命令面自描述（obligation id+holder+next 全在）、state-aware 幂等（already-active）、trailer/日期/号全工具生成。

## 2. Findings（全部 P2/注记，不影响 decision）

- **F1〔P2〕** store.Append 在 push 成功后吞掉 fetchTracking 失败（Recover 同场景返回警示错误）——观察短暂陈旧、自愈，建议对齐两处行为。
- **F2〔P2〕** derive.go historicalMergeClaim 豁免（base≠current main 的 merge claim 不做错链归属检查）为 rebind 史降噪，但任意席链上伪造的旧 base merge claim 也静默逃过归属扫描（状态不受污染——replay 只读 holder 链；纯审计可见性缺口）。建议收窄豁免条件（如仅限曾任 coordinator 的席）。
- **F3〔P2〕** checkEnforcedHCTLChange 只查「存在 required gate」，「非 author 席」实际由 config loader 的 gater≠author 拒绝间接保证——正确但跨层依赖，就地补一行自足检查更稳。
- **F4〔P2·运维含义〕** VerifyCommit 的 squash 树==head 树要求 ⇒ **merge 时 head 必须已含 main（rebase-before-merge 纪律）**；与 memo lane 高频推进 main 叠加，将常态触发 re-gate（B2 已定价的摩擦，非缺陷）。运维建议：coordinator 排好 memo 合入与 gated merge 的时序；本 PR 即活例——verdict memo 落地推进了 main，codex 须 rebase 后 delta 复审。
- **F5〔注记〕** 事件 payload 写入统一补尾换行——差分 oracle 与手写事件方需知（corpus 侧 `printf '%s'` 无尾换行，blob OID 会不同）。
- **F6〔注记〕** gitx/identity/cmd 三包无单测（identity 小、gitx 被传递覆盖）；Go JCS ↔ Python oracle 的差分互证尚未接线（与 27n F1 同源，wire 收口时补）。

## 3. 处置

- **decision=APPROVE**：零 P0/P1；F1–F3 建议随 rebase 顺手修（不强制）；F4 是设计摩擦的如实呈现；F6 并入 corpus 接线收口。
- 本 gate 绿后，p1-kernel quorum 尚欠 **grok adversarial gate**；双绿后 coordinator（claude）执行 merge——因 F4，流程为：codex rebase 至当时 main → 两 gate 对新 revision delta 复审 → main 静默窗口内 merge。
- bootstrap 注记：本 verdict 以 memo 形式落链外（refs/coop/* 未生；kernel 合入并 activation 后，verdict 走 `hctl verdict` 事件）。

## Commits 本次会话

- 本 memory（memo lane 微 PR）。
