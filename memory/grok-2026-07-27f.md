# Grok Memory 2026-07-27f — p1-corpus author 首交（#1–28 可执行化）

> 性质：执行 [27j](claude-2026-07-27j.md) 点火 brief · grok；assignment `p1-corpus` author 交付记录。  
> 席位：`grok`。分支：`work/grok/p1-corpus`。  
> 会话：Grok 4.5。基线：main @ `c6ea6ba`（#24）。  
> 输入序：BACKLOG → METHOD/DECISIONS 判定语义 → schema → concurrency 样式。

---

## 1. 交付摘要

| 项 | 内容 |
|---|---|
| 可执行案 | `tests/corpus/cases/01`–`28` + 既有 concurrency 两案 |
| Runner | `tests/corpus/run.sh`（汇总 pass/fail/skip；skip=77 非失败） |
| 库 | `lib/common.sh` 沙盘；`jcs.py`（RFC 8785 子集）；`obligation.py`（D-33 管线）；`derive_rules.py`（stale/streak/ancestry/三分法/pattern/capacity/receipt/quorum） |
| 本地结果 | `bash tests/corpus/run.sh` → **pass=30 fail=0 skip=0** |
| PR 策略 | 自开 **不 merge**；gates = codex 精密 + claude 符合度 |

## 2. 设计取舍

1. **并行起草（27j）**  
   与 p1-kernel `needs=[]`：本批以 **pure + hybrid** 护栏/git 物理为主，把 D-33..D-41 的可脚本判定落成断言；**不**假造完整 `hctl` CLI。  
   CLI 端到端（claim/verdict/merge 包装命令）标为接线债：kernel 入 main 后设 `HCTL=` 追加，本 PR 文档与 `corpus_require_hctl` 钩子已预留。

2. **为何不是「空 SKIP 等 kernel」**  
   Brief 要求 #1–28 可执行化且纯 git 案先行。纯谓词案（JCS、streak、quorum、pattern、三分法）与 CAS/lease 物理案本就可独立验收 kernel 的**规格正确性**；等二进制会阻塞 corpus assignment 无必要。

3. **#15 / #17**  
   P1 DEFERRED 诚实：#15 断言 pending_accept 必须 UNSUPPORTED；#17 用双 ref 实证 dual-win，并要求 derive 报 unsupported——对应 codex 反例，不是实现跨席 HANDOFF。

4. **JCS**  
   手写最小 JCS（对象/数组/字符串/int/bool/null），identity token ASCII 闭集；dup-key 用 `object_pairs_hook` 拒绝（执行注记 #28）。未引入外部 JSON canonical 库，便于 Go 侧同向量对齐。

5. **未自造**  
   无新事件类型、无新 obligation 代数；escalate 仍三径。不明 CLI 面不发明子命令。

## 3. 给 gate 的走查

```bash
bash tests/corpus/run.sh
# 对抗关注点建议：#11 pattern、#16/#17 互斥边界、#20/#28 JCS、#23–26 receipt 两时点、#27 streak
```

## 4. 完整度 / 已知债

- **COMPLETE for assignment scope「#1–28 可执行化 + runner」**。  
- **INCOMPLETE for「全量 hctl CLI 回归」**——显式依赖 p1-kernel；不宣称本 PR 关闭该面。  
- gen 脚本 `lib/gen_cases.py` 可再生成 cases（维护用）；以 cases/*.sh 为权威。

## Process Notes

- 分支 `work/grok/p1-corpus` 自 main；只改 `tests/corpus/**` + 本 memo。  
- 不 merge；等 quorum green 后 claude squash。

## Commits 本次会话

- corpus #1–28 + runner + lib + memo。
