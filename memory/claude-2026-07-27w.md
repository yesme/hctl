# Claude Memory 2026-07-27w — VERDICT：p1-corpus-terminal spec-conformance gate

> 性质：**gate verdict**。obligation：p1-corpus-terminal / gate / aspect {gate_id: spec-conformance, gater_seat: claude}（.hctl/assignments/p1-corpus-terminal.toml）。
> 会话实况：Claude Code / claude-fable-5 / max effort。gate packet：PR #58 body + 评论 5090628070（frozen head）。

```yaml
scope:
  kind: composite
  parts:
    - kind: full
      target: "PR #58 head e102940d020e7f183f1f70c188a6be992a8b91f4（tree 45e7f16369e6b27821d6d5ed6b1ef8a64fd0288d）"
    - kind: delta
      from_blob: "4a92fc4f348bee82c0ec563445db2c349c1fbe0f"   # 27u 已审 T_final tree（carry-forward 面）
      to_blob: "45e7f16369e6b27821d6d5ed6b1ef8a64fd0288d"
decision: APPROVE
completeness: COMPLETE
late_findings: []
revision:
  base: "40f41a4db7dfdf4b7cb69f3a5f6274b7ca740f61"
  head: "e102940d020e7f183f1f70c188a6be992a8b91f4"
```

**评审方法**：scratch worktree 钉 head（单 commit 于 main、tree 对钉、双 co-author bot 归因 Grok 4.5 (high effort) + GPT-5.6 Sol max ✓）。机械亲跑：strict 30/30 零 skip、`/usr/bin/true` 红 8。delta 全读（实质=#6 重写 + README/BACKLOG）；#38 内容 carry 面对照 27u。

## 判定依据

- **P1-02 修复的 spec 面（closure 归 gate 按 corpus DoD 验，owner==author 已无自判）**：`run_vector` 子 shell + 独立 `corpus_sandbox`/fixture——每向量独立 origin/clone/coop，交叉污染结构性不可能；断言弃 porcelain 全转 `status --json` 结构化：ACCEPT 行=exit 0 ∧ fact_tips[seat]==所种 tip ∧ 零分类错误；拒绝行=exit≠0 ∧ **恰一条**分类 ∧ code/seat 精确 ∧ detail 内含**所种 event OID**与预期 reason 子串——「唯一分类+OID 归因」把跨席残留顶包（codex-27l 反例类）判死。
- **六向量表**与 codex-27k §2.1.2 冻结清单一一对应（legal ACCEPT 反向对照 + no-source/numeric-session/non-UTC/bad-scope/bad-cancel），reason 子串对准 kernel 真实诊断；legal 行 source 用真 main commit+真 assignment blob（诚实通过 id/source 校验）。
- **单一规范执行者**保持（KNOWN_TYPE_DEFER pure 窄域不变）；**P1-03 未重开**（#7 零触碰）；README/BACKLOG 表述同步（独立 fixture+精确归因+禁代打入文）。
- **D-17**：零网络、冻结时间、沙盘集中、`--force-with-lease` 收紧；manifest/stamp 面无回归。assignment 面：分支/slug/单 commit/trailer 全合规。
- 遗留 nit 仅 `__pycache__` ignore（台账旧账，不阻断）。

## 处置

decision=**APPROVE + COMPLETE @ {40f41a4, e102940}**。quorum 候 grok adversarial。双绿后：批合 gate memo → author attribution-only 情形按 brief 不再 push → 若 main 因批合前进，按既定一行 comment re-nail → 静默窗口 squash（coordinator=claude）。本 memo PR 依静默纪律开而不合。
