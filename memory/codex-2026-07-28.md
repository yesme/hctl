# Codex Memory 2026-07-28 — PR #72 T_final owner re-gate：P1-01..03 CLOSED

> 性质：`p1-bootstrap-cutover` required `precision` gate 的 owner
> `fix_verification(P1-01..03) + delta` 报告。
> 唯一 ledger：PR #72 coordinator comment `5092344592`。
> repair round：`reconciled-repair-1`；author push budget 1/1 已由 T_final 消耗。
> 会话实况：GPT-5.6 Sol / max effort。

## 0. D-41 review result

```yaml
claimed_gate:
  assignment: p1-bootstrap-cutover
  gate_id: precision
  gater_seat: codex
gate_validity: COUNTABLE_BOOTSTRAP
gate_validity_reason: "exact assignment blob eb50f401… assigns required precision to codex; bootstrap audit debt prevents event publication but does not erase the user-issued gate"
scope:
  kind: composite
  parts:
    - kind: fix_verification
      findings:
        - codex-pr72#P1-01
        - codex-pr72#P1-02
        - codex-pr72#P1-03
      target_blob: "b659b323b0c64b0acc6f393ec5a3f70145c8cb11"
    - kind: delta
      from_blob: "0824619d041c64582a88e14500b5740a54d66d44"
      to_blob: "b659b323b0c64b0acc6f393ec5a3f70145c8cb11"
revision:
  base: "9c20adfcbc593cb905daf638a558d36d81917772"
  head: "a79d0af38d8055e5d354408add504a8ce17cd4de"
decision: APPROVE
completeness: COMPLETE
fix_verification:
  codex-pr72#P1-01: CLOSED
  codex-pr72#P1-02: CLOSED
  codex-pr72#P1-03: CLOSED
late_findings: []
repair_rounds_remaining: 0
merge_readiness: WAITING_FOR_GROK_TFINAL_GATE
```

**总判：`APPROVE + COMPLETE`；Codex owner 将 P1-01..03 全部置
`CLOSED`。** `T_final=a79d0af…` 是 `T_prev=10d333e…` 的唯一直接子提交，
tree=`b659b323…`；七路径 delta 完整落在 reconciled packet 内。上一轮四个决定性
反例和额外 non-blob 对象型反例在 T_final 全部通过；分别拆除三处关键 guard 后，
对应 targeted test / corpus 均按预期转红。

本判只完成 Codex precision obligation。PR #72 仍须 Grok 对同一 exact T_final
完成 ledger 所分配的 mutation-sensitivity adversarial + delta；两席 exact-head
双绿前不得进入 memo 批合与 merge。

## 1. `codex-pr72#P1-01` — CLOSED

### 1.1 三态与 fail-closed 已落到实现

`internal/derive/cutover.go` 不再把历史读取结果压成 `bool`：

```text
active              enforcement == "active"
proven-non-active   可解码且非 active，或 ls-tree 证明 path 不存在
unknown             blob 不可读 / 非 blob / TOML 不可解码
```

`detectCutover` 遇到 `unknown` 立即产出 `INVALID_CUTOVER error` 并返回
`cutover=nil`，所以 unknown 不可能进入 `covers`。tree cache 保存的是三态，
也不会把一次不可判定缓存成 non-active。

### 1.2 原反例、相邻路径与 mutation

Codex 在 exact T_final 重新运行带 distinct marker 的真实 Git 对象反例：

```text
bootstrap(root) → active(mid) → active(tip)
删除仅 mid 的 seats blob
结果：cutover=nil + INVALID_CUTOVER

active(root) → bootstrap(mid) → active(tip)
删除仅 root 的 seats blob
结果：cutover=nil + INVALID_CUTOVER
```

额外把 `.hctl/seats.toml` 置为 tree object，亦得到
`cutover=nil + INVALID_CUTOVER`。相邻 happy/fail-closed 路径同时成立：

- path 确不存在仍为 defined non-active；
- 正常 bootstrap→active 单翻转仍给唯一 cutover；
- garbage TOML 为 unknown/INVALID；
- rollback/double-flip 保持 INVALID。

删除 `state == enforcementUnknown` 的 fail-closed 分支后，
`TestDetectCutoverShapes` 与 `TestDetectCutoverUnknownObjectFailsClosed` 均红；
目标 guard 具有实际 mutation sensitivity。

## 2. `codex-pr72#P1-02` — CLOSED

integration tree match 现在先求 candidate anchor：沿 candidate first-parent
下降到 main 可达的最近 fork point；只有该 anchor 是 first-parent integration
commit 的 ancestor 时，tree equality 才可用于归属。candidate 已被 non-squash
merge 纳入 main、却找不到 ancestry-bound squash 时，确定落
`UNJUDGEABLE_MERGE error`，不会再回扫旧 cutover OID。

case #30 §3.5 固化原 tree-reuse 反例：

```text
post-cutover side candidate tree == covered cutover tree
post-cutover integration parents == (active tip, side candidate)
integration tree != candidate tree
```

T_final 的 oracle 为 `UNJUDGEABLE_MERGE error`，且 demo3 不出现
`ACKNOWLEDGED_BOOTSTRAP_HISTORY`；既有 pre-cutover 真
`UNRECORDED_MERGE` happy acknowledgment 仍通过。临时拆除
`snapshot.IsAncestor(anchor, entry.OID)` guard 后，case #30 在 §3.5 红于缺失
demo3 的 `UNJUDGEABLE_MERGE`，证明新增语料会杀死旧 tree-only 实现。

## 3. `codex-pr72#P1-03` — CLOSED

`markMergeDebt` 的 warning 路径已有字面 allowlist：

```go
code == "UNRECORDED_MERGE" && cutover.covers(mergedOID)
```

九行表驱动覆盖三类 debt × inside/outside/unplaced：

| debt | inside | outside / unplaced |
|---|---|---|
| `UNRECORDED_MERGE` | ACK warning，WriteGuard 放行 | 原 code error |
| `INVALID_RECEIPT` | 原 code error | 原 code error |
| `UNJUDGEABLE_MERGE` | 原 code error | 原 code error |

DECISIONS D-38 与 METHOD §10 同步撤回三类共认账的自扩 prose，明确 allowlist
恰为 `{UNRECORDED_MERGE}`。临时去掉 code guard 后，
`TestMarkMergeDebtAllowlist` 首先红于
`INVALID_RECEIPT/inside → ACKNOWLEDGED_BOOTSTRAP_HISTORY`，mutation
sensitivity 成立。

## 4. Delta

```text
base:             9c20adfcbc593cb905daf638a558d36d81917772
input head/tree:  10d333e1d62b98d05f906027fe9e1a264d06a33f
                  0824619d041c64582a88e14500b5740a54d66d44
T_final head/tree:a79d0af38d8055e5d354408add504a8ce17cd4de
                  b659b323b0c64b0acc6f393ec5a3f70145c8cb11
T_final parent:   10d333e1d62b98d05f906027fe9e1a264d06a33f
repair commits:   1
delta paths:      7
assignment blob:  eb50f401dcc2e28b55b84868df9db7cdd60a5a41 (base=head)
```

七路径均服务三条 closure：

```text
DECISIONS.md
METHOD.md
internal/derive/cutover.go
internal/derive/cutover_test.go
internal/derive/merge_history.go
tests/corpus/BACKLOG.md
tests/corpus/cases/30-bootstrap-cutover.sh
```

未见超出 ledger 的语义扩张，也未引入 late P1 finding。

两项非阻断 evidence-hygiene 注记：

1. `cutover.go:44-49` 的旧函数头注释仍写“undecodable counts as
   not-active”，与紧随其后的三态实现及 DECISIONS/METHOD 相反；执行语义无分叉，
   不值得消耗已归零的 author push budget，后续顺手清理即可。
2. repo 内 `TestDetectCutoverUnknownObjectFailsClosed` 的连续 active commit 内容
   相同，因而共享 tree/blob；它仍能杀死 unknown→false 回归，但不单独复现
   “只删 mid、tip 仍可读”的边界移动。Codex 用 distinct-marker 原反例补做了
   exact T_final 复测并通过；这是测试表达精度注记，不是 closure 缺口。

## 5. Mechanical evidence

```text
PR head / tree / parent / main freeze                       exact
GitHub compare T_prev..T_final                              ahead 1, behind 0
git diff --check                                            PASS
gofmt -d (changed Go files)                                 empty
bash -n case #30                                            PASS
shellcheck                                                  unavailable
go test ./...                                               PASS
go test -race ./...                                         PASS
go vet ./...                                                PASS
GOPROXY=off go test -mod=vendor ./...                       PASS
GOPROXY=off go vet -mod=vendor ./...                        PASS
Codex original distinct-object + tree-reuse + allowlist     PASS
non-blob seats-path precision test                          PASS
unknown guard removed                                       targeted FAIL expected
ancestry guard removed                                      case #30 FAIL expected
allowlist guard removed                                     targeted FAIL expected
HCTL=<built T_final> CORPUS_STRICT=1 tests/corpus/run.sh    PASS
strict corpus summary                                       32/32, skip=0
HCTL=/usr/bin/true strict corpus                            FAIL expected
stub summary                                                pass=22 fail=10 skip=0
GitHub Actions runs on T_final                              none
```

author push budget 已耗尽且无需续轮；Codex 不生成新的 repair packet。
