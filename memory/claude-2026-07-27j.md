# Claude Memory 2026-07-27j — P1 排班定案与两席点火文书

> 性质：排班裁决记录 + 点火 brief（指针不带语料，D-16）。用户裁决原话「没问题，按你之前的安排来」（2026-07-27）——采纳 27i §3 排班建议；本 memo 即两份 static assignment 的 `authorized_by: user` 转录（D-38 级④声明）。
> 会话实况：Claude Code / claude-fable-5 / max effort。基线：main @ `116fe2e`。
> 排班：**codex** = p1-kernel author；**grok** = p1-corpus author；gates 互换（kernel ← grok 对抗 + claude 符合度；corpus ← codex 精密 + claude 符合度）；merge coordinator = claude（seats.toml）。
> 首批 assignment 文件：`.hctl/assignments/p1-kernel.toml`、`.hctl/assignments/p1-corpus.toml`（本仓第一次真 dogfood 件；bootstrap 期自合入仓，enforcement=active 后此类变更须 gated PR）。

---

## 点火 brief · codex（p1-kernel author）

1. **身份与任务**：你是 `p1-kernel` 的 author（`.hctl/assignments/p1-kernel.toml`）。分支 `work/codex/p1-kernel`。bootstrap 期认领=开工即认。
2. **输入（按序）**：`DECISIONS.md`（D-31..D-44 是实现规格核心；D-24 语言=Go）→ `METHOD.md` 全文（normative）→ `schema/*.json` 三份 → `tests/corpus/BACKLOG.md` → 既有语料 `tests/corpus/concurrency/`（写路径参考实现即在其中）。语言细则见 `memory/claude-2026-07-27i.md` §2。
3. **交付（D-43/D-44 P1 竖切）**：Go 单 module（`cmd/hctl` + 内部包）：facts/derive、`claim`（两阶段 CAS + pending 恢复）、`verdict`、`begin`（formal 默认）、`merge`（coordinator slot + receipt）、`doctor`、`status [--json]`、`wait` 基础形；单元测试；scaffold 含 go.mod + vendored deps（TOML 库自选成熟者）+ JCS 手写（对 corpus #28 向量）+ buildinfo 自检 kernel pin。`hctl help` 与自描述输出按 D-13/D-22。
4. **流程**：完成后自开 PR **不 merge**；required gates（grok 对抗 + claude 符合度）以 memo verdict（D-41：scope + completeness 必填）判 exact `{base,head}`；quorum green 后由 claude 执行 squash merge。设计取舍落 memo `memory/codex-*.md`；不明处 escalate 不自造。
5. **围栏**：只写自己分支与 memo；trailer 取会话实况；AGENTS.md 规则全程有效。

## 点火 brief · grok（p1-corpus author）

1. **身份与任务**：你是 `p1-corpus` 的 author（`.hctl/assignments/p1-corpus.toml`）。分支 `work/grok/p1-corpus`。
2. **输入（按序）**：`tests/corpus/BACKLOG.md`（#1–28，半数是你自己要的攻击案；执行注记三条勿漏）→ `METHOD.md`（判定语义）→ `schema/*.json` → 既有两案 `tests/corpus/concurrency/*.sh`（沙盘样式基准：mktemp 一次性 repo、身份时钟冻结、退出码判定、断言 ref 状态不 grep 文案）。
3. **交付**：#1–28 可执行化（bash+git 沙盘沿既有样式；每案头注对应 D 号与出处）+ runner 薄壳（逐案跑、汇总退出码）。调用 `hctl` 二进制的案子与 p1-kernel **并行起草**（先写沙盘与断言、桩住 hctl 调用点），kernel 落 main 后接线收口——你的 PR 的 quorum green 自然发生在全绿之后。纯 git 物理案（如 #20/#27/#28 向量、#11 pattern 相交）不依赖 kernel，可先行完成。
4. **流程与围栏**：同 codex（gates=codex 精密 + claude 符合度；memo verdict；quorum green 后 claude merge；只写自己分支与 memo；escalate 不自造）。

## 备注

- 两 assignment `needs=[]`：并行起草是设计意图；集成顺序由 quorum green 的自然时序解决（corpus 绿依赖 kernel binary 在 main）。
- claude 席自留：两份符合度 gate + merge coordinator + 台账维护；不与两席抢 author。
- assignment TOML 未经机器校验入仓（本机无 tomllib、Go 侧 toml 库未 vendored——鸡生蛋期）；p1-kernel 的 doctor/loader 落地后第一件事就是回头校验这两份文件（自举闭环）。

## Commits 本次会话

- 本 memory + 两份 assignment 文件（同批微 PR，bootstrap 自合）。
