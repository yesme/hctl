# Claude Memory 2026-07-27n — VERDICT：p1-corpus spec-conformance retro-gate

> 性质：**gate verdict**（审计债 [27l](claude-2026-07-27l.md) 的 claude 半边）。obligation：p1-corpus / gate / aspect {gate_id: spec-conformance, gater_seat: claude}。
> 会话实况：Claude Code / claude-fable-5 / max effort。

```yaml
scope:
  kind: full
  target: "a2c4814 （PR #25 squash 入 main 的 p1-corpus 全量：28 案 + lib + runner + README/BACKLOG）"
decision: REQUEST_CHANGES
completeness: COMPLETE
late_findings: []
```

**评审方法**：lib 五件全文（common.sh / jcs.py / obligation.py / derive_rules.py / run.sh + gen_cases 定性）；README/BACKLOG 全文；案 1–7、10–14、16–17、20–21、23–28 全文或压缩全读；全案反模式机械扫描（网络/未冻结时钟/porcelain 文案断言/沙盘纪律）；套件双跑（无二进制与 `HCTL=` 接线各一次，均 30/30）。

## 1. 符合面（强）

- **D-17 语料纪律全项达标**：沙盘/冻结身份时钟集中于 common.sh；零网络；退出码判定；断言 ref 状态与退出码、不 grep porcelain 文案。
- **lib oracle 与定案对齐**：jcs.py 的 escape 规则、dup-key 拒绝、ASCII 范围排序正确，且如实声明不做 Unicode normalization（D-33/RFC 8785 §3.1）；obligation.py 的 domain-sep 串、preimage 闭集校验、重算比对全对（D-33）；derive_rules 的 progress 四分类、streak 状态机（黄/红/escalated 进度不清红）、stale 判据、ancestry delivered/lost、三分法核心、receipt 两时点全部与 D-34/D-37/D-39 一致。
- **git 物理案高质量**：#2（同 tip 单 CAS 域）、#4/#5（ancestry 判 delivered/lost + recovery ref）、#10（同 blob 异 parent 两事件——blob 不作 event identity）、#16（双化身 re-claim 竞争唯一赢家 + fencing）、#17 前半（跨 ref 双赢实证=D-36 DEFERRED 证据）均为真物理断言。
- **接线协议设计干净**：pure/hybrid/deferred-p2/hctl-wire 四模式 + SKIP(77) + `CORPUS_REQUIRE_HCTL` + `HCTL=` 钩子；Go 改判前旧 Python-runner 线已自清（27m 已记）。
- codex-27e 三条执行注记两条落实（#28 dup-key 检测 via `parse_i_json`；#23–26 receipt 两时点语义）。

## 2. Findings

- **F1〔P1，定 decision〕stub 同义反复段虚增覆盖面**：约 12/28 案（#8/9/12/13/14/15/18/19/24 为主，#6/27 局部）在 kernel 行为处以**案内自定义 Python stub 函数再断言自身**（frozen 拒绝、CANCEL 解冻、integrity-first、rebind fence、begin 复用、写动作 fail-closed、dedup loader 等）。这些段测试的是 stub 不是任何实现——30/30 绿因此高估了对 P1 验收面的覆盖，而「绿不得虚报」正是 D-41 的立法核心。**修复（并入既定接线收口任务，量小）**：stub 段二选一——迁到 `corpus_require_hctl` 后接真 CLI 断言；或显式标注 `wire-pending` 并使该段不参与 PASS 语义（案文注明「本段为规格示意非断言」）。
- **F2〔P2〕quorum oracle 缺第一条件**：D-41 四条件之 exact revision 未进 `quorum_counts` 也无案覆盖（#21 头注称 four conditions 实测三）；`fix_only` 分支为死代码且若被用返回错误值。
- **F3〔P2〕three_way_event 对缺失信封字段偏宽**：`schema_version` 缺失 →0→OK、接受 `v` 旧键——kernel schema 才是 authority，oracle 应对齐或注明简化。
- **F4〔P2〕obligation.py 动态分支欠 exact-keys**：`assign_event` 旁多余键不拒（additionalProperties 纪律）；动态是 P2 保留位，低危。
- **F5〔P2〕patterns_disjoint 保守假阳**：两个不同 exact 且互为前缀时误判 overlap（fail-closed 方向，自注 crude，可接受但注明）。
- **F6〔nit〕**：#14 `s2` 算而未断言；author progress 的 repo=None 走 non_forward 与 gate 路径 raise 不对称；#19 stub 以 `...` 作函数体；`corpus_event` 与 concurrency/ 原案的尾换行差异（纯 cosmetic）。

## 3. 处置

- decision=REQUEST_CHANGES 由 F1 单独驱动（threshold=P1）；F2–F6 随手修不阻塞。
- **修复归属**：grok（author），自然并入其已声明的 kernel 落 main 后接线收口（届时 stub 段本就要接真二进制——F1 的修复与之同一动作）；修复 PR 走正常 gate。
- 本 verdict 清偿审计债的 claude 半边；**codex precision retro-gate 仍欠**（27l 登记在案，待用户点火）。
- 对 27l「机械信号 30/30」的追溯校准：该信号经本审降权为「pure/hybrid 层绿」，kernel 行为覆盖待接线后重估——正与 F1 同源。

## Commits 本次会话

- 本 memory（memo lane 微 PR——`memo/claude/*` 新约定首跑）。
