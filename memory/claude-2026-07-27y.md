# Claude Memory 2026-07-27y — VERDICT：p1-attribution spec-conformance gate

> 性质：**gate verdict**。obligation：p1-attribution / gate / aspect {gate_id: spec-conformance, gater_seat: claude}。packet：PR #65 body（frozen）。
> 会话实况：Claude Code / claude-fable-5 / max effort。

```yaml
scope:
  kind: full
  target: "PR #65 head 1093c31a8db1d40c2530b098ad2a44d6e0b77b12（单 commit 于 base f98255d==当时 main）"
decision: APPROVE
completeness: COMPLETE
late_findings: []
revision:
  base: "f98255dc61be6e3f4be88fa36802334fcfbe5442"
  head: "1093c31a8db1d40c2530b098ad2a44d6e0b77b12"
```

**评审方法**：scratch worktree 钉 head；对 27r §A2 P1.1 四件逐项核；全读 identity/local.go、seat.go 解析链、trailer.go、config 校验、doctor 面、AGENTS 同步；机械亲跑 vet ✓ / go test 零 FAIL / strict corpus 31/31 零 skip（corpus 面本 PR 未触）。

## 判定依据（对 27r §A2 四件）

- **① loader**：seats 可选 `coauthor_email`（闭集解码内新增合法字段 + schema 同步）；校验非空/禁 `\r\n<>`/须含 `@`；注释明示 attribution-only、**永不进 identity/quorum**。
- **② seats.toml**：三席 bot 邮箱与 grok-27j §3.1 实测表逐字符一致（claude=281844019 / codex=281847692 / grok=302482056）。
- **③ `hctl seat init`**：写 `repo.CommonDir/hctl/local.toml`（worktree 共享 gitdir 正确定位、永不入 main 史）；LocalConfig 闭集解码（未知键拒）；idempotent；seat 探测（worktree 后缀/branch pattern/--seat）。
- **④ `hctl trailer` fail-closed**：model/effort 仅取 `HCTL_MODEL_DISPLAY`/`HCTL_REASONING_EFFORT`（session adapter 依裁留 P2），缺一 ExitGuard 且报 D-23 语义；email 链 env→local→seats 逐级校验，local seat 错配显式报错不静默；model/effort/email 全体注入字符防护；stderr 打印 email_source 供审计。
- **doctor**：attribution 行——bootstrap 期缺链 warning / active 期 error 的分级、local seat 错配 error、跨席邮箱撞车检测（doctor+seat_test 双证）。
- **AGENTS 同步**：trailer 规则改指工具链（init 写邮箱、trailer 缺一即红），旧 prose 先例段删除。
- 越界检查：零 daemon（D-13）、corpus/kernel 其余面零触碰、单 commit、grok bot trailer 合规。

## Findings（note 级，不影响 decision）

- **N1〔note〕trailer 规范形待用户加冕**：工具输出 `Co-authored-by: <model> (<effort> effort) <email>`（依 grok-27j §3.3），与各席现行手写形（如 `GPT-5.6 Sol max`、`Fable 5 max`）措辞有别。建议用户裁定工具形为唯一 canonical，各席 CLAUDE.md/AGENTS 级 prose 随之收敛——否则同仓两形并存。
- **N2〔note〕**`HCTL_COAUTHOR_EMAIL` env 覆盖为 27r 未列的逃生门：有校验、有 source 显像，方向 fail-closed，可接受；记录在案。

## 处置

decision=**APPROVE + COMPLETE @ {f98255d, 1093c31}**。quorum 候 codex precision；双绿后 coordinator 批合 memo + 静默窗口 squash。本 memo PR 开而不合。
