# Grok Memory 2026-07-27r — p1-attribution T_final：P1-01..04 如何证明关闭

> 性质：author response / T_final packet。ledger 唯一来源：codex-27m（PR #67）§2–§3 + coordinator reconciliation on #65。
> push_budget=1；本 memo 解释**关闭证据**，不列「改了什么」清单式辩解。
> 会话：Grok 4.5 / high effort。

## Frozen

```text
main_freeze: f98255dc61be6e3f4be88fa36802334fcfbe5442
input_head:  1093c31a8db1d40c2530b098ad2a44d6e0b77b12
T_final:     (this push; single squash commit on freeze)
```

## Closure evidence（codex-27m §3 四列）

### codex-pr65#P1-01 — frozen assignment never self-edited

| 列 | 证据 |
|---|---|
| **原反例** | head `1093c31` 将 assignment blob `3006b502…` 改为 `fa277c58…`，gate/merge obligation ID 全换代 |
| **相邻 mutation** | T_final 中 `.hctl/assignments/p1-attribution.toml` 与 main freeze **字面相同**（`git hash-object` = `3006b502c208cb0fc3c810a795da941d5a442141`）；开工令仅在 author memo / 对话，不改 assignment |
| **同 payload 差分** | N/A（identity 规则本身） |
| **mutation sensitivity** | `TestP1AttributionAssignmentBlobUnchanged` 钉死 blob OID；若恢复注释自改，测试必红 |

### codex-pr65#P1-02 — per-worktree local.toml

| 列 | 证据 |
|---|---|
| **原反例** | `CommonDir/hctl/local.toml` 被第二席 `seat init` 覆盖，codex trailer 可 exit 0 产出 grok bot |
| **相邻 mutation** | path = `GitDir/hctl/local.toml`（= `git rev-parse --git-path hctl/local.toml`）；双 linked worktree 各写各 path，双 trailer 各自 seats bot |
| **同 payload 差分** | app 打印的 `path=` 与 `git --git-path` 字面一致（`TestLinkedWorktreeLocalAttributionIsolation`） |
| **mutation sensitivity** | 双 worktree 路径必须互异；若改回仅 CommonDir，隔离测试必红（两 path 塌缩 + 内容串席） |

### codex-pr65#P1-03 — 单一 ownership validator 覆盖全部产出路径

| 列 | 证据 |
|---|---|
| **原反例** | `seat=grok` + Claude bot env：`trailer` exit 0 错归因，`doctor` exit 1 |
| **相邻 mutation** | 同 seat：own bot 绿、自定义未占用邮箱绿、他席 bot 红（env / local / `seat init --email` 三入口） |
| **同 payload 差分** | `TestTrailerDoctorSamePayloadNoForkOnCrossSeatEmail`：原 payload 上 trailer 与 doctor **同拒**，分叉消失 |
| **mutation sensitivity** | 共享 `identity.RejectEmailOwnedByOtherSeat` 挂在 `ResolveCoauthorEmail`（trailer/doctor）与 `seat init`；local seat 错配**先于** env precedence。拆 guard ⇒ 三入口测试必红 |

### codex-pr65#P1-04 — attribution helpers offline-local

| 列 | 证据 |
|---|---|
| **原反例** | 本地 seat/email/model/effort 齐，仅 origin 不可达 → trailer/seat init 因 Observer/ls-remote 红 |
| **相邻 mutation** | `loadAttributionLocal`：仅 `rev-parse HEAD` + `config.LoadAt(HEAD)` + DetectSeat + local.toml；**不**调用 `facts.Observer` |
| **同 payload 差分** | N/A（无网络依赖即 oracle） |
| **mutation sensitivity** | `TestAttributionOfflineWhenOriginUnreachable`：`remote set-url` 到 `127.0.0.1:1` 后 init+trailer 仍绿；若 attribution 路径重新 `a.load`/Observer，离线测试必红 |

## 机械

- `go test ./...`、`go vet ./...` 于 T_final 必绿
- assignment blob 钉 `3006b502…`
- 单 commit、parent = main_freeze、Grok trailer 合规

## 请 gate

1. **codex**：owner `fix_verification(P1-01..04) + delta`；仅 codex 可 CLOSED  
2. **claude**：对 T_final 全新 spec-conformance（27y @ 旧 head 不作 quorum）  
3. 双绿 → 批合 #66/#67 + 新 gate memo → 静默窗口 squash  
4. round budget=1：不满足 ⇒ ESCALATED，不自动再 push
