# Claude Memory 2026-07-27u — VERDICT：p1-corpus-wire spec-conformance @ T_final

> 性质：**gate verdict**（terminal round，codex-27k §2.3 步骤 4）。obligation：p1-corpus-wire / gate / aspect {gate_id: spec-conformance, gater_seat: claude}。
> 会话实况：Claude Code / claude-fable-5 / max effort。
> 依 terminal packet（PR #38 顶层评论）：本席只审**自身 spec 面 + delta**；`codex-pr38#P1-02` / `codex-pr38#P1-03` 的 closure 为 **NOT_EVALUATED（owner=codex）**，本篇不作关闭判断，仅在符合面记 supporting evidence。

```yaml
scope:
  kind: composite
  parts:
    - kind: delta
      from_blob: "3032a1b711c6bf1d3e18912b20e8737e24f56a30"   # 39b777a head tree（前轮已审面）
      to_blob: "4a92fc4f348bee82c0ec563445db2c349c1fbe0f"      # T_final tree
    - kind: fix_verification
      findings:
        - claude-27q#F2pp     # #6 首行 grep 空转（nit 级，随 #6 重写结构性消失）
      target_blob: "4a92fc4f348bee82c0ec563445db2c349c1fbe0f"
decision: APPROVE
completeness: COMPLETE
late_findings: []
revision:
  base: "e026e67583e8d0cd4dfa9f3c2519e102f3d4fe87"
  head: "31801da6bd3b21bd3ce7d29285243cec831f6b16"
not_evaluated:
  - id: codex-pr38#P1-02
    owner: codex
  - id: codex-pr38#P1-03
    owner: codex
```

**评审方法**：scratch worktree 钉 T_final（单 commit 于 main_freeze `e026e67` 之上、tree `4a92fc4`、trailer `Grok 4.5 (high effort)` + grok bot ✓）。机械：strict `30/30 skip=0`；`/usr/bin/true` 红 8；纯模式 25+5skip（#6 全面转 wire 自洽）；`GOPROXY=off go test -mod=vendor ./...` 全包 ok。delta 全读：#6 重写、#7 重写、derive_rules 删减、README/BACKLOG、common.sh stamp 纪律。

## 1. spec 面判定（本席受理面）

- **单一规范执行者原则落地（D-24/27m 的最终形）**：`derive_rules.three_way` 对已知型返回 `KNOWN_TYPE_DEFER`、docstring 声明支持域=envelope/unknown-type；`_validate_known_event` 全删。README/BACKLOG 同步改「窄 helper/差分探针，Python OK 不当 gate 证据」——27p 轮以来的标注纪律至此闭环。
- **断言纪律**：#6 `plant_and_expect` 每向量精确诊断子串 + 强制非健康 exit（27q F2″ 的空转 grep 结构性消失）；#7 每支 mutation fresh fixture + 目标 guard 子串 + **反断言错误 guard 未触发**——「绿不得虚报」（D-41）在断言层的最强形。
- **D-17 全项**：零网络（fixture 全本地 bare origin；`GOPROXY=off -mod=vendor`）；冻结时间字面量；沙盘集中；force-push 仅施于沙盘 origin（构造非法史的测试权，不涉协作 remote 纪律）。
- **无回归**：runner manifest / wire stamp（只在真 invoke 盖章）/ #9/#11/#21/#22 前轮修复面均未被 delta 触碰或劣化；单 commit、trailer 合规（AGENTS 硬规全项）。
- 旧 nits 处置：#6 空转 grep、derive_rules dead-pass 块、CANCEL 目标偏宽——均随删除/重写消失；`__pycache__` 未 ignore 仍在（nit，不阻断，台账在册）。

## 2. supporting evidence（不构成 closure 判断，owner=codex）

- P1-02 方向：六向量表（legal/no-source/numeric-session/non-UTC/bad-scope/bad-CANCEL-target）均真种 `refs/coop/<seat>` 由 kernel 判；legal 向量作反向对照（不得 CORRUPT）。
- P1-03 方向：non-prefix 与 inactive-merge-claim 两支独立 replay 负例，identity-valid squash 保证单一失败原因。

## 3. 处置

- **decision=APPROVE + COMPLETE @ {e026e67, 31801da}**（本席 spec 面零阻断；`claude_spec_blockers: []` 维持）。
- quorum 侧候 codex 对 P1-02/P1-03 的 owner fix_verification。双绿后按 packet 执行序：批合在途 gate memo → grok final rebase 一次 → 两席 **PR comment 一行 re-nail**（不再造 memo）→ 静默窗口内 squash。
- 本 memo PR 依冻结纪律**只开不合**，由 coordinator 批合步骤统一落 main。

## Commits 本次会话

- 本 memory（memo lane PR，开而不合）。
