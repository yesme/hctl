# Codex Memory 2026-07-27n — PR #65 owner re-gate：ESCALATED

> 性质：`p1-attribution` required `precision` gate 的 owner
> `fix_verification(P1-01..04) + delta` 报告。
> 用户授权：2026-07-27「owner fix_verification(P1-01..04) + delta」。
> 会话实况：GPT-5.6 Sol / max effort。
> 唯一 ledger：PR #65 coordinator comment `5091352765`；本轮
> `author_push_budget=1` 已由 T_final 消耗。

## 0. D-41 review result

```yaml
claimed_gate:
  assignment: p1-attribution
  gate_id: precision
  gater_seat: codex
gate_validity: COUNTABLE_BOOTSTRAP
gate_validity_reason: "frozen assignment on exact PR base assigns required precision to codex; existing p1-corpus audit debt prevents event publication but does not erase the user-issued bootstrap gate"
scope:
  kind: composite
  parts:
    - kind: fix_verification
      findings:
        - codex-pr65#P1-01
        - codex-pr65#P1-02
        - codex-pr65#P1-03
        - codex-pr65#P1-04
      target_blob: "432a9eb97d20a79cab50e75202784125dc564054"
    - kind: delta
      from_blob: "a4fdcfe4d5d55b80f008d5ef0d250a37a2ec7522"
      to_blob: "432a9eb97d20a79cab50e75202784125dc564054"
revision:
  base: "f98255dc61be6e3f4be88fa36802334fcfbe5442"
  head: "64a4b5f8b20a8e41b7ffd032e20e77bb5265283b"
decision: REQUEST_CHANGES
completeness: COMPLETE
disposition: ESCALATED
fix_verification:
  codex-pr65#P1-01: CLOSED
  codex-pr65#P1-02: CLOSED
  codex-pr65#P1-03: ADDRESSED
  codex-pr65#P1-04: CLOSED
closure_denied:
  - codex-pr65#P1-03
late_findings: []
repair_rounds_remaining: 0
```

**总判：`REQUEST_CHANGES + COMPLETE / ESCALATED`。** P1-01、P1-02、P1-04
已经满足冻结 closure；P1-03 的 producer guard 已修，但 `doctor` 在当前
`enforcement=bootstrap` 下把同一 ownership failure 降成 warning/exit 0，故
`trailer` 与 `doctor` 仍分叉。新增的 no-fork 测试未断言 doctor 非零，且其最后条件
在前置断言后逻辑不可达，因此实现坏而测试仍绿。

按唯一 ledger 的 round budget，本判不授权第二次 Grok push，也不生成另一份横向
repair packet。后续仅由用户决定是否特批一轮、修改冻结 invariant，或终止该 PR。

## 1. P1-03 — closure denied：分叉被反向，测试假绿

ledger 要求：

```text
seat init / env / local / seats resolution / trailer / doctor 同一判定
同 payload trailer/doctor 分叉必须消失
拆 shared guard 时三入口测试必红
```

T_final 的 `ResolveCoauthorEmail` 与 `RejectEmailOwnedByOtherSeat` 已让所有 producer
在产出前拒绝他席 bot；这部分方向正确。但 `internal/app/doctor.go:165-171` 对
`ResolveCoauthorEmail` 的**所有**错误统一套用：

```go
status := "warning"
if enforcement == "active" {
    status = "error"
}
```

本仓当前 enforcement 正是 bootstrap。exact T_final、完整远端事实、同一原始
payload 的独立 CLI 复测：

```text
seat=grok
HCTL_COAUTHOR_EMAIL=281844019+a-claude-code-bot[bot]@users.noreply.github.com

hctl trailer:
  exit 1
  ERROR: email is configured for seat "claude", not current seat "grok"

hctl doctor:
  exit 0
  attribution warning: email is configured for seat "claude", not current seat "grok"
```

因此分叉从旧 head 的 `trailer=0 / doctor=1` 变成了
`trailer=1 / doctor=0`，并未消失。缺失 attribution chain 在 bootstrap 可 warning，
不等于已知他席 bot、local seat mismatch 也可降级；后两者是确定的错误事实。

新增测试 `internal/app/seat_test.go:242-257` 也没有证明 author packet 声称的
“同拒”：

1. `tCode == 0` 已在 244–246 行立即 `Fatal`；
2. 255 行却只在 `tCode == 0 && dCode != 0` 时失败，故该分支永远不可达；
3. 全测试没有要求 `dCode != 0`，只要求 doctor 文本包含错误原因。

于是真实 `doctor exit 0` 时
`TestTrailerDoctorSamePayloadNoForkOnCrossSeatEmail` 仍 PASS。按 D-41，
required-gate 目标实现坏而验收仍绿本身即 P1。P1-03 只能记为 author
`ADDRESSED`，Codex owner 拒绝置 `CLOSED`。

若用户特批继续，原冻结 closure 的最小充分条件仍是：known-other-seat ownership 与
local-seat mismatch 在 doctor 中无条件为 `error`；测试同时断言 trailer/doctor
均非零且 attribution status=`error`。这不是新 finding，也没有扩大 scope。

## 2. P1-01 / P1-02 / P1-04 — CLOSED

### P1-01 CLOSED

- base 与 T_final 的 `.hctl/assignments/p1-attribution.toml` blob 均为
  `3006b502c208cb0fc3c810a795da941d5a442141`；
- PR diff 已无 assignment 变更，旧 obligation/receipt identity 不再自换代；
- mutation test 对恢复旧注释自改会红。

### P1-02 CLOSED

独立双 linked-worktree 复测：

```text
grok  path=<common>/.git/worktrees/grok-wt/hctl/local.toml
codex path=<common>/.git/worktrees/codex-wt/hctl/local.toml
```

两路径分别与各自 `git rev-parse --git-path hctl/local.toml` 字面一致；先后 init 后，
Grok trailer 仍取 Grok bot、Codex trailer 仍取 Codex bot。`GitDir` 替代
`CommonDir`，原覆盖反例与相邻 mutation 均被锁住。

### P1-04 CLOSED

在本地 HEAD、seat/email/model/effort 全齐后把 origin 改为
`https://127.0.0.1:1/unreachable.git`，`seat init` 与 `trailer` 均 exit 0，并产出
正确 Grok bot。调用链改为 `loadAttributionLocal`，不再经过
`facts.Observer/ls-remote`；恢复远端依赖时命令级测试会红。

## 3. Delta 与机械核验

```text
old head/tree: 1093c31a8db1d40c2530b098ad2a44d6e0b77b12
               a4fdcfe4d5d55b80f008d5ef0d250a37a2ec7522
new head/tree: 64a4b5f8b20a8e41b7ffd032e20e77bb5265283b
               432a9eb97d20a79cab50e75202784125dc564054
base:          f98255dc61be6e3f4be88fa36802334fcfbe5442
commits:       1
delta paths:   9
```

完整读取 `1093c31..64a4b5f` 九路径 delta，并复核最终 16-path PR surface。除
P1-03 closure 自身外，无新增 late P1 finding。

```text
go test ./...                                      PASS
go vet ./...                                       PASS
go test -race ./...                                PASS
HCTL=<built T_final> CORPUS_STRICT=1 corpus/run.sh PASS (31/31, skip=0)
gofmt -d                                           empty
git diff --check                                   FAIL
```

`diff --check` 仅报 `memory/grok-2026-07-27r.md:61-63` 的 Markdown 行尾空格，
属非阻断 hygiene；未参与 P1 判定。决定性反例是 exact CLI 的
`trailer exit 1 / doctor exit 0`，且作者同名 targeted test 仍 PASS。

## 4. Escalation

本轮 push budget 已耗尽，P1-03 未满足唯一 ledger。PR #65 禁 merge；Codex 不向
Grok 发第二份修复清单。用户需在以下方向中裁决：特批一个严格限于 P1-03 的额外
repair push、显式修改 closure invariant 接受 doctor warning，或终止/重派本项。
