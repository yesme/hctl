# Grok Memory 2026-07-27s — T_final2：P1-03 窄修（用户特批）

> 性质：exceptional author push packet。authority=user 特批一次；scope 仅 `codex-pr65#P1-03`。
> 输入 head：`64a4b5f8b20a8e41b7ffd032e20e77bb5265283b`。P1-01/02/04 保持 CLOSED 不动。
> 会话：Grok 4.5 / high effort。

## 为何足以关闭（原 payload + 相邻）

### 原反例（codex-27n 独立 CLI）

```text
seat=grok
HCTL_COAUTHOR_EMAIL=<claude bot>
enforcement=bootstrap

T_final:  trailer exit 1 / doctor exit 0 (attribution warning)  ← 分叉反向
T_final2: trailer exit 1 / doctor exit 1 (attribution error)    ← 同拒
```

doctor 对 `identity.IsHardAttributionConflict`（ownership + local-seat mismatch）在 **bootstrap/active 一律 `error`**；仅「链缺失」仍可 bootstrap warning。

### 相邻 happy/fail

| payload | trailer | doctor attribution |
|---|---|---|
| grok + claude bot env | ≠0 | error / exit≠0 |
| grok + local seat=claude | ≠0 | error / exit≠0 |
| grok + own bot / unowned custom | 0 | ok（既有绿路径不变） |

### 测试 mutation sensitivity

`TestTrailerDoctorSamePayloadNoForkOnCrossSeatEmail` 现**真实断言**：
- `tCode != 0` 且 `dCode != 0`
- attribution 行含 `error` + ownership / mismatch 文案
- 去掉不可达的 `tCode==0 && dCode!=0` 假绿分支

若 doctor 再把 hard conflict 降为 warning → 测试必红。

### 未触碰（held fixed）

- assignment blob `3006b502…`
- per-worktree `GitDir/hctl/local.toml`
- `loadAttributionLocal` 离线路径

## 请 gate

- **codex**：仅 P1-03 owner fix_verification + `64a4b5f→T_final2` delta  
- **claude**：同 head 重钉 spec-conformance  
- 本 push 后 exceptional budget=0；不满足即再 ESCALATED
