# Codex Memory 2026-07-27o — PR #65 T_final2 owner re-gate：P1-03 CLOSED

> 性质：`p1-attribution` required `precision` gate 的 owner
> `fix_verification(P1-03) + delta` 报告。
> 用户授权：2026-07-27「特批一次」；唯一 amendment 为 PR #65 comment
> `5091536977`。
> 会话实况：GPT-5.6 Sol / max effort。

## 0. D-41 review result

```yaml
claimed_gate:
  assignment: p1-attribution
  gate_id: precision
  gater_seat: codex
gate_validity: COUNTABLE_BOOTSTRAP
gate_validity_reason: "frozen assignment on exact PR base assigns required precision to codex; existing p1-corpus audit debt prevents event publication but does not erase the user-issued bootstrap gate"
scope:
  kind: composite
  parts:
    - kind: fix_verification
      findings:
        - codex-pr65#P1-03
      target_blob: "0b593ffd5cb57ebda6f00c0bcee34f8d8cad4616"
    - kind: delta
      from_blob: "432a9eb97d20a79cab50e75202784125dc564054"
      to_blob: "0b593ffd5cb57ebda6f00c0bcee34f8d8cad4616"
revision:
  base: "f98255dc61be6e3f4be88fa36802334fcfbe5442"
  head: "e0c0c941da0a5f7e06f30fbf0040e9385d36cd6d"
decision: APPROVE
completeness: COMPLETE
fix_verification:
  codex-pr65#P1-03: CLOSED
held_fixed:
  codex-pr65#P1-01: CLOSED
  codex-pr65#P1-02: CLOSED
  codex-pr65#P1-04: CLOSED
late_findings: []
exceptional_author_pushes_used: 1
exceptional_author_pushes_remaining: 0
merge_readiness: WAITING_FOR_CLAUDE_EXACT_HEAD_RENAIL
```

**总判：`APPROVE + COMPLETE`；`codex-pr65#P1-03` 由 owner 置
`CLOSED`。** T_final2 在原冻结 invariant 内消除了 producer/doctor 分叉，新增测试
能杀死旧实现；`64a4b5f…→e0c0c94…` 五路径 delta 未引入 late P1 finding。
P1-01/02/04 的已关闭事实在本 delta 上保持不变。

本判只完成 Codex 席的 owner closure。PR #65 仍须 Claude 对同一 exact head
`e0c0c941…` 重钉 spec-conformance，之后才满足双绿条件。

## 1. P1-03 — CLOSED

### 1.1 同一判定已落到确定错误类型

T_final2 为两类确定冲突建立 sentinel：

- `ErrEmailOwnedByOtherSeat`
- `ErrLocalSeatMismatch`

`RejectEmailOwnedByOtherSeat` 与 local-seat mismatch 路径均保留
`errors.Is` 链；`doctor` 在 enforcement 分级前调用
`identity.IsHardAttributionConflict`。因此 known-other-seat ownership 与
local-seat mismatch 在 bootstrap/active 均直接成为 `error`，只有非 hard conflict
才可能沿既有 bootstrap warning 分支。

该改动没有复制第二套 ownership oracle：producer 仍经
`ResolveCoauthorEmail` / `RejectEmailOwnedByOtherSeat`，doctor 复用同一 resolution
结果并只分类错误强度。

### 1.2 原 payload 与相邻路径

在 exact T_final2 构建二进制，并令隔离远端 `main=e0c0c941…` 后独立运行：

```text
payload: seat=grok
         HCTL_COAUTHOR_EMAIL=<Claude bot>
         enforcement=bootstrap

trailer_rc=1
ERROR: coauthor email owned by another seat ...

doctor_rc=1
attribution error: coauthor email owned by another seat ...
```

原来的 `trailer=1 / doctor=0+warning` 分叉已消失。相邻 fail-closed 与 happy
路径同时成立：

```text
local.toml seat=claude, current seat=grok:
  trailer_rc=1
  doctor_rc=1
  attribution error: local attribution seat mismatch

local.toml seat=grok, own Grok bot:
  trailer_rc=0
  doctor_rc=0
  attribution ok
```

### 1.3 测试不再假绿，mutation 能杀旧实现

`TestTrailerDoctorSamePayloadNoForkOnCrossSeatEmail` 现在分别断言：

1. trailer nonzero；
2. doctor nonzero；
3. attribution 行为 `error`，并包含 ownership 原因；
4. local-seat mismatch 同样为双 nonzero + attribution error。

旧的不可达条件 `tCode == 0 && dCode != 0` 已删除。把 T_final2 的新测试原样装到
旧实现 `64a4b5f…` 上，targeted test 会失败于：

```text
doctor attribution must be status=error (not warning)
```

因此 mutation sensitivity 来自 attribution status 的实质断言，不依赖 incidental
doctor exit。原 payload、相邻 fail/happy、共享 validator 与 mutation kill 四项共同
满足 amendment 的 closure contract。

## 2. Held-fixed 与 delta

```text
input head/tree: 64a4b5f8b20a8e41b7ffd032e20e77bb5265283b
                 432a9eb97d20a79cab50e75202784125dc564054
new head/tree:   e0c0c941da0a5f7e06f30fbf0040e9385d36cd6d
                 0b593ffd5cb57ebda6f00c0bcee34f8d8cad4616
base:            f98255dc61be6e3f4be88fa36802334fcfbe5442
delta commits:   1
delta paths:     5
```

五路径仅为 doctor 分类、sentinel/wrapping、对应测试与 Grok packet：

```text
internal/app/doctor.go
internal/app/seat.go
internal/app/seat_test.go
internal/identity/local.go
memory/grok-2026-07-27s.md
```

P1-01 的 assignment blob 在 base/head 均为
`3006b502c208cb0fc3c810a795da941d5a442141`。P1-02 linked-worktree isolation、
P1-04 unreachable-origin，以及 attribution producer 的既有回归均重跑通过；本次
delta 没有改回 CommonDir 或引入 Observer。

唯一非阻断 hygiene 是 Grok packet 两行 Markdown 行尾空格；完整 PR diff 另继承
前一 packet 三行同类空格。它们不改变实现、测试或 closure 判定，不构成 late P1。

## 3. Mechanical evidence

```text
targeted attribution/assignment/linked/offline tests       PASS
old implementation + new no-fork test                      FAIL (expected)
go test ./...                                               PASS
go vet ./...                                                PASS
go test -race ./...                                         PASS
gofmt -d (four changed Go files)                            empty
HCTL=<built T_final2> CORPUS_STRICT=1 tests/corpus/run.sh   PASS
corpus summary                                               31/31, skip=0
```

复核结束时 exceptional push budget 为 0；Codex 不授权也不需要下一轮 repair。
