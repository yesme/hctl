# Grok Memory 2026-07-27t — VERDICT：p1-bootstrap-cutover adversarial @ freeze

> 性质：required gate 报告。assignment=`p1-bootstrap-cutover`，gate=`adversarial`，threshold=`P1`。
> 冻结 packet（author freeze tip，开审）：base / head / tree 如下。scope=`full`（验收面=codex-27k §3.3 六条）。
> 镜头：误认账 / 静默 INVALID / 改史逃逸。会话：Grok 4.5 / high effort。

## 0. Frozen revision

```text
PR:     #72 feat(kernel): bootstrap cutover — 有界认账 bootstrap 审计债
base:   9c20adfcbc593cb905daf638a558d36d81917772
head:   10d333e1d62b98d05f906027fe9e1a264d06a33f
tree:   0824619d041c64582a88e14500b5740a54d66d44
parent(head) == base: yes (single commit)
assignment blob base==head: eb50f401… (p1-bootstrap-cutover.toml 未自改)
scope: full
completeness: COMPLETE
decision: APPROVE
max severity seen: none (no P1)
```

## 1. Preflight

- tip 钉死；diff 10 paths，无 assignment 自改、无顺手扩 scope。
- trailer：Fable 5 max + claude bot ✓。
- 机械（detached exact-head）：`go test ./internal/derive/ ./internal/app/` PASS；`go vet ./internal/derive` PASS；`HCTL=<built head> CORPUS_STRICT=1 tests/corpus/run.sh` → **32/32 skip=0**；case #30 单独 PASS。

## 2. §3.3 六条 × adversarial 攻击结果

| # | 冻结验收条 | 攻击意图 | 结果 |
|---|---|---|---|
| 1 | 唯一 bootstrap→active cutover | 双翻转 / 回退 / 中段 garbage 伪装 | 形状 `active^k·non-active^m`；回退/双翻转 → `INVALID_CUTOVER` error；garbage/missing=not-active，只收紧不扩认账（单测+corpus 4/5） |
| 2 | cutover∪祖先 UNRECORDED → ACK warning，WriteGuard 放行 | 认账是否伪造 completed / 抹原 code | `markMergeDebt` 仅降 severity+改 AuditDebt code 为 ACK；detail 保留原 code+cutover OID；**Completed 仍 false**；WriteGuard 单测放行 |
| 3 | cutover 后债仍 error | 后债能否被 tip-first 错绑进认账集 | `covers` 不含 tip-after-cutover；corpus #30 段 3 post-flip UNRECORDED error 与 ACK 并存 |
| 4 | activation∈边界（反循环） | tip=cutover 时 tip 是否被错误排除 | k≥1 时 cutover=`history[k-1]` 在 acknowledged 内；与法条一致 |
| 5 | 歧义 fail-closed | INVALID 能否静默 / 仅 warning | `INVALID_CUTOVER` 恒 `SeverityError`；无 soft path；k=0/全 active 返回 nil（无假 cutover） |
| 6 | 法条：有界边界≠repair | D-38/METHOD 是否空转 | 同批 D-38 增补 + METHOD §10 段；corpus/README 声明义务仍 open |

## 3. 镜头专项

### 3.1 误认账（false ACK）

- **空 OID / 无法定位 first-parent**（`mergedOID==""` UNJUDGEABLE）：不 covers → 仍 error（单测 unplaced）。
- **无 cutover（bootstrap 期 / genesis-active）**：`cutover==nil` → 全量 error 路径。
- **post-cutover OID**：boundary 测试明确 tip 不 covers。
- **宽松 seats 解码**：缺失/garbage=not-active；作者自证方向只会触发响亮 INVALID 或推迟 cutover，不静默扩大 acknowledged 集——与代码一致。

### 3.2 静默 INVALID

未发现 soft-warn 或吞错路径。形状失败只产 `INVALID_CUTOVER` error problem；corpus 段 4/5 要求 exit≠0 + code 存在。

### 3.3 改史 / tree 逃逸

- **强制改 enforcement 史**（active→bootstrap→active）：形状检测红，不能靠改 tip seats 单独逃逸。
- **tree 缓存**（`cache[entry.Tree]`）：enforcement 在 tree 内，同 tree 同判定——不构成跨 enforcement 串味。
- **squash tree 碰撞**（head tree 撞上 pre-cutover main tree）：属既有 merge 鉴定语义（tip-first 取最新同 tree）。cutover 只改变「已放置债」的 severity。若 post-cutover **新** merge OID 在 first-parent 上，仍按 covers(OID) 判定，不能仅凭 tree 继承旧 ACK。**不升 P1**：无证据表明 post-cutover 新 OID 可被误 covers；tree 碰撞假合并是 cutover 前就存在的鉴定面，本 PR 未放宽 placement 谓词。记 **N1** 给 P2/precision 旁注即可。

## 4. Notes（不阻断）

- **N1** tree-identity squash 鉴定 × ACK 叠加：建议未来若收紧 merge 放置谓词，与 cutover 正交单独立项。
- **N2** corpus #30 自陈 post-flip 可能 STALE_BINARY；WriteGuard 放行以 unit 为准——诚实，不构成 false-green of classification。

## 5. Disposition

```yaml
decision: APPROVE
completeness: COMPLETE
scope: full
findings: []
notes: [N1 tree-placement pre-existing, N2 STALE_BINARY honesty]
blocking: false
```

候 codex precision 同 freeze；双绿后批合 gate memo、静默窗口 squash。本席不对 precision findings 代 CLOSED。
