# Claude Memory 2026-07-27x — VERDICT：p1-regate-carry spec-conformance gate

> 性质：**gate verdict**。obligation：p1-regate-carry / gate / aspect {gate_id: spec-conformance, gater_seat: claude}。packet：PR #62 body（frozen）。
> 会话实况：Claude Code / claude-fable-5 / max effort。

```yaml
scope:
  kind: full
  target: "PR #62 head 7bed0cae2cf65833d51856f94100f94f2337cd25（单 commit 于 base 44e8717==当时 main）"
decision: APPROVE
completeness: COMPLETE
late_findings: []
revision:
  base: "44e871711f62d4620a6675436ea4f0cb73ce4a30"
  head: "7bed0cae2cf65833d51856f94100f94f2337cd25"
```

**评审方法**：scratch worktree 钉 head。carry.go 全文精读（安全边界）；derive.go 集成 diff 全读；DECISIONS（D-04/05/35/37/40/41）与 METHOD §8.1–8.3 修文对照 codex-27k §3.1 冻结规格逐条核；corpus #29 与 run.sh manifest；carry_test 负向量表；status 显像。机械亲跑：`go vet` ✓、`go test ./...` 全绿（3 包 no-test 已知）、strict corpus **31/31 零 skip**、`/usr/bin/true` 红 **9**（#29 入 wire 集）。

## 判定依据（对 27k §3.1 逐条）

- **candidate_fp 忠实**：域分隔 `hctl-candidate-fingerprint-v1\0` + 全字段长度前缀（无拼接歧义）；author 身份=name/email 字节精确（仅剥 timestamp/tz 且格式严校验）；message 全字节（含 trailer）；tree delta=sorted(path, 双侧 mode/type/OID + presence 位)——whitespace/binary/mode/rename-as-delete-add 全保留；**恰一 parent** 否则不 carry（merge/非线性拒）；环/不可读对象 fail-closed。**patch-id 依裁决绝迹**。
- **谓词八条全实现且 fail-closed**：绿票三连（APPROVE+COMPLETE+scope 覆盖）、is_ancestor(B,B')、同 obligation/PR、claim 未变且非 escalated（不改 fencing token，D-35 修文自洽）、最新 verdict 语义（更新的 RC 自然阻断旧绿 carry）、fingerprint 等价、base delta ⊆ `ValidMemoPath`（memory/ 前缀+clean+禁控制字符）、与 candidate paths 不相交。exact verdict 永远优先于 carry。
- **不铸事件不改史**：carry 为 derive 派生态；JSON `carried:{kind,verdict,claim,report,revision}` + status 人读显原 verdict OID/revision/report 三元组——原证据呈现达标。
- **法条上迁忠实**：D-04（exact 默认+唯一窄例外、旧 verdict 永不改写）、D-05/D-37（freshness 指针改 D-04/D-40）、D-35（不铸新事件注记）、D-40/D-41 amended；METHOD §8.1（fingerprint+八条件全文）、§8.2（ownership/corpus-DoD/closure 五件套）、§8.3（reconciliation barrier、一轮预算、ESCALATED、收口指标）——27s §3 + 27k round budget 全量上迁，memory 不再是唯一现行规则。
- **corpus/单测**：#29 hctl-wire 正支（memo-only 前进→carried→**carried quorum 直通 merge --check**）+ 负支（whitespace/commit-message/non-memo-base 断 no-carry）；manifest 冻结集入 29；carry_test 负向量含 whitespace/message/file-mode/author-identity/non-linear/source-base-advance/RC/INCOMPLETE/newer-verdict/different-pr。
- 零 findings（含 nit）。

## 处置

decision=**APPROVE + COMPLETE @ {44e8717, 7bed0ca}**。quorum 候 grok adversarial；双绿后 coordinator 照 §4 协议批合 memo + 静默窗口 squash。本 memo PR 开而不合。
