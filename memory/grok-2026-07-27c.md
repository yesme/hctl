# Grok Memory 2026-07-27c — 封口核验轮 verdict（27f 契约）

> 性质：**封口核验签收**（非重审）。任务书 [27f](claude-2026-07-27f.md)；底本 [27e](claude-2026-07-27e.md)。  
> 席位：`grok`（对抗堂）。彼此独立、先落盘；未读 codex 本轮产出。  
> 会话：Grok 4.5。基线：`origin/main` @ `bc65722`（#12）。  
> 范围严格按 27e §5 / 27f：(a) 本堂转录 checklist；(b) 六项新增设计；(c) scope+completeness。范围外不重开。

---

```
scope: {
  kind: fix_verification+delta,
  base: 27e blob 700aa807691b36a8b3f40753aea672744e44f896
}
```

**blob 核验**：`git rev-parse origin/main:memory/claude-2026-07-27e.md` = `700aa807691b36a8b3f40753aea672744e44f896` — 与 27f 声明一致。

---

## 1. checklist（grok-27b 钉死清单 / 条件 → 27e 落位）

| # | 诉求（grok-27b） | 判 | 落位核对 |
|---|---|---|---|
| 1 | B1：禁纯 blob（稳定名+blob）/aspect 闭集/hash 规格 | **✓** | §2.1：`assignment.{id,blob}` 双料；aspect 按 kind 语法钉死；`sha256:` + domain-sep `hctl-obligation-v1\0` 全长禁截断；preimage 与 id 双存 |
| 2 | B2：merge claim 前置 quorum 与授权 | **✓** | §2.2-2/-4/-9：只派 coordinator；claim 前置 quorum green 级②；self-merge 澄清。**强于**二轮「P0 一句」——串行承重改 coordinator+capacity=1（codex 反例成立，本堂接受为忠实升级而非偏离） |
| 3 | B2：先严后松版本化/禁旋钮/橡皮图章/非 squash fail-closed | **✓** | §2.2-7/-8/-10 逐条在场；摩擦账引用本堂 k≲5 估算 |
| 4 | B4：assignment 文件 gate + 伪造转录诚实句 | **✓** | §2.3：`.hctl/**` required gate≥1 非 author；`authority:user`=级④声明非证明 |
| 5 | B5：HANDOFF 原子形态 | **✓** | §2.4 两形态裁定（开放面①）；双空窗口按 CAS 域关闭 |
| 6 | C9：窗口/进度重置/escalate 后 frozen | **✓** | §3.6：窗口=(assignment rev, obligation_id) 自上次终结；进度=可验证 git 事实；红+escalated；frozen 拒 re-claim；解冻三径 |
| 7 | 链治理：tip-keyed 缓存+增量校验 | **✓** | §3.7 职责拆分句在场；compaction 仍不设计 |
| 8 | B1 churn 风暴可见性 | **✓** | §2.1 末：`assignment_revision moved` + gate + 级④点名 |
| 9 | escalate 不隐式 ASSIGN / draft 不占 slot / C1 crash 安全 | **✓** | §2.3 schema 补+绕过表；§3.1 末句悬空对象可 GC 重做安全 |
| 10 | corpus 11/13/14/16 | **✓** | §4 表 #11/#13/#14/#16 均在；#15/#16 覆盖 pending_accept 与双化身 re-claim |

**checklist 小结**：10/10 **✓**。无 ✗。二轮「必须钉死」与有条件项均被落位；B2 从 merge-as-obligation 升为 coordinator 是对 codex 阻断的重写，**不削弱**本堂 quorum 门与版本化条件，记为忠实吸收。

---

## 2. 六项新增设计

### ① HANDOFF 两形态 — **APPROVE**

- **同席换化身零协议事件**：claim 席级资产（K-26）⇒ 无 release 窗口 ⇒ 一审「双空」物理消失。正确。  
- **跨席两步 `pending_accept`**：关旧 claim + 指向目标；他席不可抢；目标显式 CLAIM 引 HANDOFF OID；超窗 overdue 回池。无双空、有 custody 链。  
- 对抗抽查：from 已 HANDOFF、to 未接时 obligation ≠ unclaimed —— 写动作拒抢，符合。P2 实装 / P0 定状态机时间盒可接受。  
- **无 REQUEST_CHANGES。**

### ② escalated=推导态 + 解冻三径 — **APPROVE**

- 无 escalate 事件类型 ⇒ 刷屏 DoS 面消失（K-06）。  
- 解冻仅 CANCEL(claim)/CANCEL(obligation)/新 ASSIGN —— 无第四暗门。  
- NOTE(category=escalation) advisory 不碰 gate/slot —— 与 NOTE 法律一致。  
- **无 REQUEST_CHANGES。**

### ③ coordinator=claude + `.hctl/**` 过渡例外 — **APPROVE**

- bootstrap 绑 claude：主笔席 + 单 CAS 域论证（§2.2-3）足够；改绑走 gated PR。  
- P1 前自开自合例外：单操作者 dogfood 诚实窗口；**写明随 P1 merge wrapper 交付自动终止** —— 可接受。  
- 残留运维风险（非本轮否决）：「P1 交付」若只靠人口头，例外可能悬挂。见 §3 逸出 backlog（不阻塞 COMPLETE）。  
- **无 REQUEST_CHANGES。**

### ④ 红线计数取齐 — **APPROVE**

- 首 re-claim（第 2 个 accepted CLAIM）= 黄：期望 failover。  
- 第 2 次 re-claim（第 3 个 accepted CLAIM）无插入进度=红+escalated。  
- 与二轮「≥2 次 re-claim / 有进度重置」同构且更可执行（accepted-only、CAS 输家不落链）。  
- **无 REQUEST_CHANGES。**

### ⑤ obligation preimage 形态 — **APPROVE**

- 语义五元组：`assignment.id` + `assignment.blob` + `kind` + `target` + `aspect`（顶层 JSON 四键、assignment 内双字段——与 27f「五键」表述一致于语义层）。  
- domain-sep + 全长 sha256；动态 assign_event 分支清楚。  
- `canonical_json` 规范化算法标 P0 —— 合法遗留，不阻塞封口（schema 落地时钉）。  
- **无 REQUEST_CHANGES。**

### ⑥ 评审收敛纪律 — **APPROVE**

- 范围单调收缩（full → fix+delta）可机械定义；verdict 必填 scope+completeness；quorum 只认 COMPLETE=级② —— 直接可执行。  
- 逸出程序避免「禁晚期发现=带病合入」与「无限重开」两端。  
- 对合法发现的杀伤：有 late-finding 通道；不自动 stale 他人 —— **不过严**。  
- 本轮即该纪律首跑：范围遵守中。  
- **无 REQUEST_CHANGES。**

---

## 3. completeness / 逸出

```
completeness: COMPLETE
```

本范围内（checklist 10 行 + 六项新增）**无保留发现**；六项均为 APPROVE；无 REQUEST_CHANGES。

### 逸出（late-finding，不阻塞本轮，入 backlog）

| 标 | 内容 | 逸出原因 |
|---|---|---|
| late-finding | 建议 P1 交付时 `doctor` 硬检「bootstrap 自开自合例外已关」（或 seats/config 显式 `merge_wrapper=ready`），避免 ③ 的过渡例外靠人口头悬挂 | 属 ③ 的运维闭合细节；27e 已写「随 P1 交付自动终止」，机器触点未钉——上轮条件清单未要求 doctor 旗，故非转录 ✗；本轮范围外增强 |

其余二轮已同意项不重开。

---

## 4. 对抗堂签收一句话

**27e @ `700aa807…` 对本堂二轮条件为忠实落位；六项新增设计全部 APPROVE；封口核验 COMPLETE。**  
建议：两堂均 COMPLETE 后用户终裁 → DECISIONS 转录 → P0 开工。

---

## Process Notes

- 对齐：`git reset --hard origin/main` @ `bc65722`（席位分支与 squash 史分叉后的标准复位）。  
- 读序：27f → 27e（对照 grok-27b 清单）→ 本 verdict；未读 codex-27c（若有）。  
- 写围栏：仅 `memory/grok-2026-07-27c.md`。  
- 日期 2026-07-27；既有 a/b → 本篇 `c`。

## Commits 本次会话

- 本 memory（微 PR 自开自合 squash）。
