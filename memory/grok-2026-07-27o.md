# Grok Memory 2026-07-27o — VERDICT：p1-corpus-terminal adversarial gate（PR #58）

> 性质：**required gate verdict**（assignment `p1-corpus-terminal` / gate id `adversarial` / seat `grok`）。
> 对象：PR https://github.com/yesme/hctl/pull/58 · 分支 `work/codex/p1-corpus-terminal`。
> 会话：Grok 4.5 (high effort) / 对抗镜头。
> 输入：PR body + 统一 gate brief（issue comment frozen head）+ head 树 `tests/corpus/**` + assignment `.hctl/assignments/p1-corpus-terminal.toml` + DECISIONS D-17/D-24/D-39/D-41 + codex-27k §2.1。
> 验证：detached scratch @ exact head；`GOPROXY=off go test/race/vet`；strict corpus 30/30；`HCTL=/usr/bin/true` 红 8 案；单案 #6 亲跑 PASS。

---

## 0. Verdict（D-41 / D-40）

```yaml
gate_id: adversarial
gater_seat: grok
assignment: p1-corpus-terminal
decision: APPROVE
completeness: COMPLETE
threshold: P1   # review parameter only; kernel reads decision only (D-40)
scope:
  kind: composite
  parts:
    - kind: fix_verification
      findings:
        - codex-pr38#P1-02   # known-event false-green / foreign-seat substitute
      target_blob: "45e7f16369e6b27821d6d5ed6b1ef8a64fd0288d"
    - kind: full             # full PR surface per gate brief
revision:
  base: "40f41a4db7dfdf4b7cb69f3a5f6274b7ca740f61"
  head: "e102940d020e7f183f1f70c188a6be992a8b91f4"
report: memory/grok-2026-07-27o.md
late_findings: []
```

**总判：APPROVE + COMPLETE。**
P1-02 假绿（跨席/宽 grep 代打）在对抗面闭合；P1-03 按 brief **不重开**。零 P0/P1 阻断。

**exact revision 核验：**

- `git rev-parse origin/work/codex/p1-corpus-terminal` = `e102940d020e7f183f1f70c188a6be992a8b91f4`
- `HEAD^{tree}` = `45e7f16369e6b27821d6d5ed6b1ef8a64fd0288d`（与 brief 一致）
- `merge-base --is-ancestor 40f41a4d… HEAD` 成立
- 单 commit；trailer 含 Grok bot + Codex bot（GitHub 可识别 co-author）

---

## 1. 对抗审查范围与方法

| 面 | 动作 |
|---|---|
| 身份 / assignment | `p1-corpus-terminal` author=codex、gates=grok adversarial + claude spec；branch_slug 对齐 |
| P1-02 假绿 | 读 #6 全表；攻击 foreign-seat 代打、裸 CORRUPT、错 OID/seat/code/reason |
| 规范权威 | `three_way` 仅 DEFER；README 无「Python 镜像 Go union」 |
| 机械 | unit/race/vet、strict 30/30、anti-stub 红 8、`git diff --check` |
| 范围纪律 | P1-03 代码仍在 carry-forward 中，本 gate **不**重审其负例闭合 |

未做：真实 `gh pr merge`；跨机双 clone 人工对射（有 store 单测同构）。

---

## 2. 攻击结果（按严重度）

### P0 — 无

### P1 — 无阻断

| 攻击 | 结果 |
|---|---|
| 同 fixture 多向量串味（旧假绿） | **挡住**：`run_vector` 每行 `corpus_sandbox` 子 shell + 独立 origin/coop |
| 他席残留 CORRUPT 顶包本行 | **挡住**：`len(classifications)==1` 且 `seat`/`tip`/`code`/`reason` 全匹配；禁止裸 `CORRUPT` |
| 健康 legal-claim 被全局红态误杀 | **挡住**：`ACCEPT` 要求 exit 0、无 CORRUPT/UNSUPPORTED、fact_tips[seat]==tip |
| Python `OK` 冒充 kernel | **挡住**：known type → `KNOWN_TYPE_DEFER`；正负例均 `hctl status --json` |
| 错 reason / 错 code 仍绿 | **挡住**：`expected_detail in detail` 且 code 精确 |
| tip 不在 detail 仍绿 | **挡住**：`tip in problem.detail`；kernel 写 `eventOID: reason`（chains.go） |

### P2 — 残留（不阻塞 APPROVE）

1. **legal-claim 健康判定依赖 exit 0 全盘**
   若未来 fixture 引入与本事件无关的 SeverityError，ACCEPT 行会假红。当前 demo fixture 干净；可后续收窄为「仅 classification 问题为空」。
2. **VERDICT bad-scope 用 fake assignment blob**
   故意坏 scope 时 source 非 fixture 真 blob——可接受（目标是 parser corrupt）；非本 finding 面。
3. **#7 仍在 PR 面（carry-forward）**
   按 brief 不重开 P1-03；未做对抗重测其 guard 子串（交给已 CLOSED 证据 + 本轮 30/30 回归）。

---

## 3. fix_verification：codex-pr38#P1-02

| checklist（27k §2.1 / brief） | 本轮 |
|---|---|
| known-event 唯一规范执行者 = Go | ✓ README + DEFER + wire-only 裁决 |
| #6 六向量独立 fixture + plant | ✓ legal / no-source / numeric-session / non-utc / bad-scope / bad-cancel |
| 精确 OID/seat/code/reason | ✓ JSON problems 单点匹配 |
| 无 HCTL 时不 Python 冒充 PASS | ✓ wire 段 `corpus_require_hctl`；anti-stub 红 |
| no-source + 相邻 mutation 保留 | ✓ no-source + session/UTC/scope/cancel |

**对抗结论：P1-02 CLOSED 可主张（owner=codex 可写 CLOSED；本席仅 APPROVE 本 gate）。**

---

## 4. 机械 transcript

```text
revision:
  base 40f41a4db7dfdf4b7cb69f3a5f6274b7ca740f61
  head e102940d020e7f183f1f70c188a6be992a8b91f4
  tree 45e7f16369e6b27821d6d5ed6b1ef8a64fd0288d

GOPROXY=off go test -mod=vendor ./...        PASS
GOPROXY=off go test -race store/derive/protocol/receipt PASS
GOPROXY=off go vet -mod=vendor ./...         PASS
git diff --check base..head                  PASS
HCTL=<built> CORPUS_REQUIRE_HCTL=1 run.sh    pass=30 fail=0 skip=0
HCTL=/usr/bin/true CORPUS_REQUIRE_HCTL=1     exit 1 fail=8
HCTL=… cases/06-unsupported-facts.sh         PASS
```

---

## 5. 处置

- **decision=APPROVE**：adversarial 面无 P0/P1；P1-02 假绿路径在攻击表上闭合。
- 候 **claude** `spec-conformance` 对同一 frozen revision。
- 双绿后：batch memo（本篇 + claude report）→ author final rebase if needed → PR comment re-nail → coordinator merge。
- bootstrap：本 verdict 为 memo 载体；合 main 后 report 指针方可达。

## Commits 本次会话

- 本 memory（memo lane 微 PR）。
