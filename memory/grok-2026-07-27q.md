# Grok Memory 2026-07-27q — p1-attribution 实现（A2 四件）

> 性质：author 交付记录。assignment `p1-attribution`；开工令：用户「p1-attribution 开工」。
> 范围锚定：claude-27r §A2 + grok-27j。会话：Grok 4.5 / high effort。

## 交付

1. **loader**：`Seat.CoauthorEmail` + schema + `ValidateSeats` 可选校验（不安全字符 / 缺 @）。
2. **seats.toml**：三席 bot 邮箱（claude / codex / grok）按 27j §3.1。
3. **`hctl seat init`**：写 `.git/hctl/local.toml`（seat、coauthor_email、machine_alias）；default←seats；`--email` / `--machine-alias`；idempotent。
4. **`hctl trailer` fail-closed**：model/effort←env only（session adapter=P2）；email←`HCTL_COAUTHOR_EMAIL`→local→seats；任一不可得 exit≠0。stderr 报 `email_source`。
5. **`hctl doctor` attribution**：缺失（bootstrap=warning / active=error）、local seat 错配=error、邮箱撞上他席 bot=error。
6. **帮助 / AGENTS** 同步；assignment 注记清除开工冻结。

## 非目标（刻意不做）

- session 证据 adapter（P2）
- commit-msg hook / `hctl commit`
- 裸 `hctl init` 仓脚手架

## 验收（单测覆盖）

- Resolve 优先级 env > local > seats；seat 错配红
- seat init → local.toml → trailer 形固定
- doctor 跨席 bot email 红
- coauthor_email 解码/校验；local round-trip

## Gate 请求

- codex：**precision**
- claude：**spec-conformance**（对 27r §A2）
