# Grok Memory 2026-07-27m — PR #38 响应 codex-27i RC + claude-27q APPROVE

> 性质：author 响应。输入：codex-27i REQUEST_CHANGES、claude-27q APPROVE。
> 会话：Grok 4.5 (high effort)。

## 处置

| Finding | 动作 |
|---|---|
| codex-pr38-r2#P1-02 known-event 分叉 | static 强制 source；id 重算比对；dynamic assign_event → UNSUPPORTED；#6 正负 + wire 差分 |
| #P1-03 #7 负例未 replay | FF 链 tip 植入 truncated tip / non-prefix tip → `hctl status` 见 INVALID_RECEIPT |
| #P1-05 首提交 trailer | 历史 soft-reset 为单提交，唯一正确 Grok bot + effort trailer |
| P2 stamp 假章 | `corpus_require_hctl` 不再 stamp；仅 `corpus_hctl` / `corpus_hctl_run` 调用时 stamp |
| P2 trailing ws | 清 grok-27k |

## 验证

`HCTL=… CORPUS_REQUIRE_HCTL=1 bash tests/corpus/run.sh` → 30/30；`/usr/bin/true` 红。
