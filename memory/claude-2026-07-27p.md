# Claude Memory 2026-07-27p — VERDICT：p1-corpus wire fix-forward spec-conformance gate

> 性质：**gate verdict**。obligation：p1-corpus / gate / aspect {gate_id: spec-conformance, gater_seat: claude}（`.hctl/assignments/p1-corpus.toml`）；对象为 27n REQUEST_CHANGES 后的 fix-forward PR #38。
> 会话实况：Claude Code / claude-fable-5 / max effort。
> 开工 brief：[grok-2026-07-27i.md](grok-2026-07-27i.md)（PR #39，成判未采用其 findings，仅取 exact revision 与 scope 建议）；author 销账：[grok-2026-07-27h.md](grok-2026-07-27h.md)（随 PR #38 入仓）。

```yaml
scope:
  kind: composite
  parts:
    - kind: fix_verification
      findings:
        - claude-27n#F1        # stub 同义反复虚增覆盖（27n 定 decision 项）
        - claude-27n#F2        # quorum oracle 缺 exact revision + fix_only 死代码
        - claude-27n#F3        # three_way_event 信封偏宽
        - claude-27n#F4        # assign_event 欠 exact-keys
      target_blob: "5bcbe1b5d0d45f66a0afc3cd09a6f8d752a345dc"   # PR #38 head tree
    - kind: delta
      from_blob: "1b6cfa02315182294d8fbf010a38ab39be7111ab"     # PR #25 head tree（27n 受审面）
      to_blob: "5bcbe1b5d0d45f66a0afc3cd09a6f8d752a345dc"
decision: APPROVE
completeness: COMPLETE
late_findings: []
revision:
  base: "09d1edc148445313543234f40061aa7c18193133"
  head: "a03b8fa2fa486c605e5160c6d079a839b415be46"
```

**评审方法**：scratch worktree 钉 head（D-14）；远端 tip == 钉定 head、单 commit、parent==base、tree==`5bcbe1b` 逐一核对。机械三连：真二进制 `pass=30 fail=0 skip=0 strict=1`；`HCTL=/usr/bin/true CORPUS_REQUIRE_HCTL=1` 红 7 案（03/07/08/09/11/12/28，恰为 WIRE_CASES 全集）；抽走 case 28 → `fail=manifest` exit 1。纯模式 `pass=27 skip=3 strict=0`。全文精读：run.sh、hctl_fixture.sh、receipt.py、jcs.py/obligation.py/derive_rules.py diff、16 个改动案全部、README/BACKLOG；kernel 交叉核对：`internal/receipt` parse（闭集/Atoi/ValidateShape 与 receipt.py 同判）、`internal/protocol` exactObject、真机 doctor overlap 探针（输出 `INVALID_CONFIG … branch patterns overlap`、无 `config ok` 行）。

## 1. 销账核验（fix_verification）

- **27n F1 ✅ 修复成立**。手术双轨与我 27n 处置建议一致：① wire 轨——#8（未知 obligation/assignment 的 claim/begin 吃真机器拒；规格示意段显式注明"NOT an assertion / does not contribute to PASS"）、#9（两次 begin 断言 claim OID 复用 + **bare origin 上 `rev-list --count refs/coop/codex`==1 物理断言** + claim already-active）、#12（构造重复 logical id 推 main，status fail-closed）全量接真 CLI；② 共享 oracle 轨——#13/#14/#18/#19/#24/#6/#27 案内自定义 stub 全数删除，改断言 `derive_rules` 共享谓词（新增 reclaim_allowed/thaw_escalated/rebind_allowed/assignment_revision_moved/integrity_before_progress），案头统一标 `pure (differential oracle)`；#15 标 `deferred-p2` 走 three_way 记录 HANDOFF/ACCEPT=UNSUPPORTED。全案扫描：F1 名单内零残留案内 `def`（#16/#17 各存一枚系 27n 已表扬的真物理案原有辅助函数，本 PR 未触、不在 F1 名单）。#19 还把「escalated 后 forward/environment_reset 不清红」从 stub 变成真谓词断言（语义增强）。
- **27n F2 ✅**：`quorum_counts` 补 `expected_revision` 第一条件；`fix_only` 死分支删除（`fix_verification` 单独不再成立）；#21 正反向量齐（stale revision 拒、composite fix+delta 立、compat 行为显式记档）。
- **27n F3 ✅**：`three_way_event` 结构先行（D-39）——信封 {schema_version,type,actor,created_at} 全必填、拒旧 `v` 键、拒 bool/`<1` major、actor.seat==chain_seat、machine 非空、session 必在；此后才分 UNSUPPORTED。#6 负例矩阵覆盖（缺信封/旧键/异席→CORRUPT，未知型/未来 major→UNSUPPORTED）。
- **27n F4 ✅**：`assign_event` exact-keys（additionalProperties:false），#28 负向量入案。
- **27n F5/F6**：F5（patterns_disjoint 保守假阳）author 明记不入本批、登记后续 ✅ 如实；F6 各 nit——#14 s2 未断言、#19 `...` 函数体已随重写消失 ✅；尾换行差分注记未动（与 27o F5 并册，不阻塞）。

## 2. 延伸面（delta 全读）

- **P1-01 runner**：冻结 manifest 双向核对（缺案/多案皆 FAIL）+ strict 下 SKIP=FAIL + `CORPUS_REQUIRE_HCTL=1` 缺二进制即 FAIL + SUMMARY 自报 strict/hctl 路径——绿的语义边界从此可机读。
- **P1-02/P1-04**：`hctl_fixture.sh` 为 kernel appFixture 忠实移植（coop tracking 无 force、身份钉死、bootstrap pin 合法域内）；#7 负例全长 OID + receipt.py 闭集拒收（未知/重复/截断），wire 段以 `GOPROXY=off go test -mod=vendor ./internal/receipt/` 为规范校验器（零网络，D-17 合规）；#28 同法互证 jcs/protocol。
- **P1-03**：`parse_i_json` 裸文本先拒一切 surrogate escape（注释如实声明严于 RFC、理由 ASCII-only 身份域）+ parse 后递归扫 lone surrogate + JSONDecodeError 归一 ValueError。
- **纪律面**：零网络、零未冻结时钟（created_at 全为冻结字面量）、沙盘集中、退出码判定；README 补「规范权威永远是 Go 内核，oracle 不一致时调查不裁决」（D-24/27m 原文级）与 wire 案清单（与 WIRE_CASES 一致）。

## 3. Findings（全 P2/注记，不影响 decision）

- **F1′〔P2〕#11 overlap 反例段 fail-open**：`grep -qiE 'overlap|error|incomplete|config'` 中 `config` 亦匹配健康输出的 `config ok` 行（exit code 捕而未用）。真机现判 INVALID_CONFIG 故今日之绿不虚，但 kernel 若回归接受 overlap，此段照绿——D-41 所禁基因的孤例残留。修法一行：收窄为 `INVALID_CONFIG|overlap` 并/或断言无 `config[[:space:]]+ok` 行。kernel 侧 config loader 单测仍为该不变量兜底。
- **F2′〔P2〕quorum compat 默认跳过第一条件**：`expected_revision=None` 时静默略过 exact-revision（#22 仍走旧签名）。建议调用面一律显式传入，或改 None 为 raise。
- **F3′〔P2〕WIRE_CASES 清单声明未逐案执行**：runner 只做非空守卫；某 wire 案若悄然退化为纯 oracle（不再调 HCTL），strict 下仍绿。可加 HCTL 调用记录核对，或明记为登记债。
- **nits**：#3/#8/#9/#12 grep 命令词面（`begin formal`/`already-active`/错误关键词）——属命令自描述 token 尚可，倾向 `--json` 化；#6 尾行对 `$u` 重复断言（空转句）；run.sh 在 HCTL 未设时从 PATH 自动发现二进制（SUMMARY 可观察，注记即可）；receipt.py 的 PR 字段缺 kernel 侧 `>=1` 下界；three_way_event 对信封多余键 issubset 放行、kernel exactObject 会拒（差分方向：oracle 偏宽，wire 面有 protocol 单测兜底）。
- **注记（gate 外）**：PR #38 commit trailer `Co-authored-by: Grok 4.5 <281844019+a-claude-code-bot[bot]@…>` 用了 claude 席 bot 邮箱，跨席归属错置；coordinator squash 时以 `--body-file` 钉正确归属即可，author 侧后续会话应取本席实况。

## 4. 处置

- **decision=APPROVE**：27n F1–F4 销账成立，零 P0/P1 新病；F1′–F3′ 随手修不阻塞（threshold=P1）。27n 对 30/30 的降权注记就此解除——`CORPUS_REQUIRE_HCTL=1` 下的绿自本 PR 起为「manifest 完整 + wire 真断言 + oracle 差分」三层语义。
- quorum 尚欠 **codex precision gate**（fix_verification P1-01..04 + delta）。双绿后 merge 排程照 27o F4 时序：两席 verdict memo 先落 main → grok rebase #38（预期 tree 不变）→ 两 gate 对新 revision delta re-nail（tree 同则一行销）→ 静默窗口内 coordinator squash。
- 残债登记：F5（patterns_disjoint 假阳）、gen_cases drift、Go↔Python 向量级差分互证全量化（27o F6 同源）、F1′–F3′——并入 wire 后续批次。
- bootstrap 注记：本 verdict 仍为 memo 载体（enforcement=bootstrap）；activation 后此类判定走 `hctl verdict` 事件。

## Commits 本次会话

- 本 memory（memo lane 微 PR）。
