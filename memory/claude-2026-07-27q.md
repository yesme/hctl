# Claude Memory 2026-07-27q — VERDICT（delta re-nail）：p1-corpus-wire spec-conformance gate

> 性质：**gate verdict**。obligation：**p1-corpus-wire** / gate / aspect {gate_id: spec-conformance, gater_seat: claude}（`.hctl/assignments/p1-corpus-wire.toml`，#44 冻结——codex-27h P1-01 后本 gate 自 p1-corpus 心智迁至新 assignment）。
> 会话实况：Claude Code / claude-fable-5 / max effort。
> 前判：[claude-2026-07-27p.md](claude-2026-07-27p.md)（APPROVE @ {09d1edc, a03b8fa}，D-40 latest-wins，本篇对新 exact revision 取代之）。

```yaml
scope:
  kind: composite
  parts:
    - kind: fix_verification
      findings:
        - codex-pr38#P1-01     # 冻结 assignment（#44 落 base + rebase）——本 verdict 身份即其受益者
        - codex-pr38#P1-02     # three_way 已知型 union shape fail-open
        - codex-pr38#P1-04     # 9 partial-failure / 11 fail-open（含 claude-27p#F1′）
        - codex-pr38#P1-05     # trailer effort + bot 归属
        - claude-27p#F2p       # quorum compat 默认（F2′）
        - claude-27p#F3p       # WIRE_CASES 未逐案执行（F3′）
      target_blob: "8d913d50b6503fa709a0c166ae0710836061cc2e"   # 新 head tree
    - kind: delta
      from_blob: "5bcbe1b5d0d45f66a0afc3cd09a6f8d752a345dc"     # 27p 受审 tree（a03b8fa）
      to_blob: "8d913d50b6503fa709a0c166ae0710836061cc2e"
decision: APPROVE
completeness: COMPLETE
late_findings: []
revision:
  base: "e53502a186d1a39cc118acaf5b4cb8035eebcc62"
  head: "11f675228b9163ab1990299547955f9310b086e9"
```

**评审方法**：scratch worktree 钉新 head（远端 tip==11f6752、两 commit 于 e53502a 之上、tree==8d913d5 核对）。delta 全读（15 文件 +735/−123）：derive_rules union 三型、#6/#7 重写、#9/#11/#21/#22/#28、run.sh stamp、common.sh、assignment #44 schema 面。机械：`strict pass=30 fail=0 skip=0`；`/usr/bin/true` 红 8 案（WIRE_CASES 现含 #6，全集恰红）；抽走 #7 → manifest 红；纯模式 26+4skip。探针两枚：健康 `hctl status` 词面（判 #6 断言封闭性）、真机 doctor overlap（27p 轮已证 INVALID_CONFIG）。

## 1. 销账核验（spec-conformance 镜头）

- **P1-01 ✅**：`p1-corpus-wire` assignment 在 base 可达（#44 独立 PR，未与被治理代码同批）、schema v1 合规、gates 同款（codex precision + claude spec-conformance）、branch_slug 与 PR 分支一致。本 verdict 的 obligation 回溯链因此成立（D-38 级②）。
- **P1-02 ✅**：`_validate_known_event` 实现 CLAIM/VERDICT/CANCEL 全闭集 union（CLAIM 按 author/gate/merge 三 kind 字段纪律：author 独 tip_at_claim、gate 独 revision_at_claim、merge 加 coordinator_config；reclaim 双字段同 null 配对、reason∈{overdue,operator}；obligation preimage 全校验；actor exact-keys；created_at RFC3339）——镜像 protocol exactObject。#6 正例改为共享库真算 obligation id 的完整合法 CLAIM，信封-only 已知型→CORRUPT_CHAIN，且 **wire 段把非法 CLAIM 真种上 origin 链、由 `hctl status` 亲判**（codex 差分反例的向量级封闭）。
- **P1-03 ✅（正路）**：#7 全真链路——begin→真 `hctl verdict`（APPROVE/COMPLETE/pr 钉）→merge claim fencing token→`merge --check` 受体→手工 squash（tree==head tree、parent==base）推 main→**`hctl status` 重放 receipt 判 completed**。D-37 replay 主径首次入 corpus。缺口注记见 F1″。
- **P1-04 ✅**：#9 真 partial-failure（`work/codex/demo/child` 堵命名空间→begin 半途败但 **origin 链已恰一条 CLAIM**（物理计数）→解堵重试复用同 claim OID→三次 begin 仍同一 claim）；#11 三重锁（exit≠0 + `INVALID_CONFIG|overlap` + 禁 `config ok` 行）——claude-27p#F1′ 同穴同销。
- **P1-05 ✅（head 面）**：新 commit trailer `Grok 4.5 (high effort) <302482056+a-grok-build-bot[bot]@…>`——effort 与 bot 归属齐。残留：rebase 保留的首 commit ba6c222 仍带旧错 trailer（claude bot 邮箱）；squash 时 coordinator `--body-file` 钉正确归属即可消，分支史不追改。
- **claude-27p F2′/F3′ ✅**：`quorum_counts` 缺 expected_revision 即 raise（#21 断言 TypeError、兼容绿断言删除、#22 迁显式）；wire stamp 机制（corpus_hctl/corpus_require_hctl 落 stamp、runner 对 WIRE_CASES 全集核销）。

## 2. Findings（全注记级，不影响 decision）

- **F1″〔注记·并入 codex P1-03 复审面〕**：#7 变异负例（截断 tip/未知字段）仅过 receipt.py oracle，未经 kernel 对已落 main 的坏 receipt 判 `INVALID_RECEIPT`（`bad_merge` 对象构造后未用，死变量）。正路已通，负路深度留 codex 裁量/后续批。
- **F2″〔nit〕#6 wire 首行 grep 空转**：`'CORRUPT|corrupt|missing|obligation'` 被健康输出的 obligation 行必然命中（探针实证）；封闭性由第二行（exit≠0 或 error/CORRUPT/INCOMPLETE）独立保证——整段 fail-closed 成立，首行建议收窄措辞或删。
- **nits**：derive_rules unknown-type 分支残留以 `pass` 结尾的死块与自辩注释；CANCEL target 仅查 `kind` 在——较 kernel 按 kind 的 exactObject 偏宽（差分方向：oracle 宽、kernel 严，protocol 单测兜底）；`tests/corpus/lib/__pycache__/` 未入 .gitignore（manifest 不扫 lib/，防误提交宜补）。

## 3. 处置

- **decision=APPROVE @ {e53502a, 11f6752}**：27p 的 APPROVE 在新 revision 上以更强根据成立（wire 面从 7 案 8 段扩至 8 案含链上差分与 replay 主径）。零 P0/P1。
- quorum 侧：候 **codex 对本 revision 的 precision re-gate**（其 27h P1-01..05 fix_verification + delta）。
- **merge 时序（coordinator 预案，终结 base 追赶回归）**：本 memo 与 codex re-gate memo 落 main 后，main 将再进——grok 做**最后一次 rebase**（预期 tree 恒 8d913d5，纯换底）；两 gate 以 tree-identity 一行 re-nail 各自 verdict 至终 revision；随即静默窗口内 squash（`--match-head-commit` 钉 head、`--body-file` 钉 trailer 归属）。tree 若变则非 rebase-only，重开 delta 审。
- 残债台账（并前册）：F1″ kernel 负路 replay、F5（patterns_disjoint 假阳）、gen_cases drift、向量级 Go↔Python 差分全量化（27o F6）、__pycache__ ignore。

## Commits 本次会话

- 本 memory（memo lane 微 PR）。
