# Codex Memory 2026-07-27l — PR #38 terminal precision gate：ESCALATED

> 性质：`p1-corpus-wire` required `precision` gate 的 terminal
> `fix_verification(P1-02,P1-03) + delta` 报告。
> 会话实况：OpenAI Codex / `gpt-5.6-sol` / `max` effort。
> 对象：PR #38 `work/grok/p1-corpus-wire`；审查在 exact head 的 detached scratch
> worktree 完成，未修改 author 分支。
> 依据：PR #38 顶层 `Terminal reconciliation packet` 与
> [codex-2026-07-27k.md](codex-2026-07-27k.md) §2；本轮
> `author_push_budget=1` 已耗尽。

## 0. D-41 review result

```yaml
claimed_gate:
  assignment: p1-corpus-wire
  gate_id: precision
  gater_seat: codex
gate_validity: COUNTABLE_BOOTSTRAP
gate_validity_reason: "frozen assignment is reachable from the exact PR base and assigns precision to codex"
scope:
  kind: composite
  parts:
    - kind: fix_verification
      findings:
        - codex-pr38#P1-02
        - codex-pr38#P1-03
      target_blob: "4a92fc4f348bee82c0ec563445db2c349c1fbe0f"
    - kind: delta
      from_blob: "3032a1b711c6bf1d3e18912b20e8737e24f56a30"
      to_blob: "4a92fc4f348bee82c0ec563445db2c349c1fbe0f"
revision:
  base: "e026e67583e8d0cd4dfa9f3c2519e102f3d4fe87"
  head: "31801da6bd3b21bd3ce7d29285243cec831f6b16"
decision: REQUEST_CHANGES
completeness: COMPLETE
disposition: ESCALATED
fix_verification:
  codex-pr38#P1-02: OPEN
  codex-pr38#P1-03: CLOSED
late_findings: []
repair_rounds_remaining: 0
```

**总判：`REQUEST_CHANGES + COMPLETE`，assignment 依 terminal round budget 转
`ESCALATED`。** P1-03 已机械关闭；P1-02 的单规范执行者方向已修正，但六向量 wire
验收仍可借别席残留的 `CORRUPT_CHAIN` 假绿，未满足冻结 checklist 中“每份 raw
payload 由 kernel 精确分类”。这不是新增 invariant 或 late finding，而是
codex-27k §2.1 明文 closure 的 fix-verification 失败。

本判不授权第二次 Grok push，也不生成下一份横向 repair packet。后续只有用户可选择
继续一轮、取消/重派，或修改 assignment scope；在此之前 PR #38 禁 merge。

## 1. P1-02 — OPEN：六向量共享红态，后五支并未各自证明 kernel 分类

`derive_rules.py` 已删除 known CLAIM/VERDICT/CANCEL 的 full-union validator，known
type 返回 `KNOWN_TYPE_DEFER`；README/BACKLOG 也已明确 Go kernel 是唯一规范执行者。
方向正确。

阻断发生在 `cases/06-unsupported-facts.sh` 的 wire 证明链：

1. 六个向量共用一个 fixture，非法事件会留在其他 `refs/coop/<seat>`；
2. `plant_and_expect` 每次读取的是包含**所有席位问题**的全局 `hctl status`；
3. numeric-session、non-UTC、bad-scope、bad-cancel 的正则均允许裸
   `CORRUPT`，且只检查全局 nonzero；
4. 因此第一支 no-source 留下的 Grok `CORRUPT_CHAIN` 足以替后续目标向量代打。

最小反例没有修改候选代码，只复用同一 fixture：

```text
1. 在 refs/coop/grok 植入 no-source CLAIM；
2. 把 numeric-session 目标替换成 session=null 的结构合法 Claude CLAIM；
3. 调用与原脚本相同的 hctl status 与匹配式。

status_exit=1
original_regex='session must be a string or null|CORRUPT' => match
target_reason='session must be a string or null'          => miss
sole CORRUPT_CHAIN                                      => seat=grok,
  CLAIM: static obligation requires source
```

也就是说，目标 numeric-session 已被 kernel 接受时，原断言仍会 PASS。后续三支同样
处于累积红链之后，具有同一假绿结构。合法 CLAIM 支路也只排除
`CORRUPT_CHAIN`，未排除 `UNSUPPORTED_FACTS`，没有独立锁住已知合法事件的接受分类。

因此 strict corpus 的 `30/30` 只能证明 runner 执行了 #6，不能证明冻结的六向量
分类矩阵具有 mutation sensitivity。`codex-pr38#P1-02` 保持 OPEN。

## 2. P1-03 — CLOSED：两支 mutation 到达各自 historical replay guard

本轮 exact head 实跑并用 trace 核对：

- happy path 继续得到 merge obligation `completed`；
- full-length non-prefix fact tip 使用独立 happy-path fixture，integration commit
  保持 `parent == Hctl-Base`、tree 等于 head tree，精确得到
  `receipt fact tip ... is not a prefix of current facts`；
- inactive merge claim 使用另一独立 fixture，精确得到
  `receipt merge claim ... is not active (active=...)`；
- 两支均明确拒绝 `identity fields do not match` 代打，且 body 先通过 shape parse。

目标 guard 若被旁路，预期诊断子串即消失，用例会红；故
codex-27k §2.2 的 happy path、fresh replay、两支独立 mutation、具体诊断与
mutation sensitivity 均成立。`codex-pr38#P1-03` CLOSED。

`corpus_sandbox` / `corpus_hctl_fixture` 在两支 mutation 前各有一次随后被
`run_happy_path` 覆盖的冗余初始化，且 trap 只清最后一个目录；这是 fixture hygiene
的 P2，不改变 fresh-fixture 隔离与本轮 decision。

## 3. Delta 与机械核验

```text
remote PR revision:
  base       e026e67583e8d0cd4dfa9f3c2519e102f3d4fe87
  head       31801da6bd3b21bd3ce7d29285243cec831f6b16
  merge-base e026e67583e8d0cd4dfa9f3c2519e102f3d4fe87
  tree       4a92fc4f348bee82c0ec563445db2c349c1fbe0f
  commits    1
  trailer    PASS (Grok 4.5 / high effort / Grok bot)

GOPROXY=off go test -mod=vendor -count=1 ./...        PASS
GOPROXY=off go test -mod=vendor -count=1 -race ./...  PASS
GOPROXY=off go vet -mod=vendor ./...                  PASS
bash -n corpus scripts                                PASS
python3 -m py_compile corpus libs                     PASS

plain corpus:
  pass=25 fail=0 skip=5 strict=0
real hctl strict corpus:
  pass=30 fail=0 skip=0 strict=1
HCTL=/usr/bin/true strict corpus:
  exit=1; pass=22 fail=8 skip=0

targeted #6 / #7:
  original scripts PASS
  P1-02 false-green probe REPRODUCED
  P1-03 prefix guard / active-claim guard REACHED

git diff --check base..head:
  FAIL — memory/grok-2026-07-27n.md lines 3–4 trailing whitespace
GitHub checks:
  none reported
```

Whitespace、#7 冗余 sandbox 与既有 `gen_cases.py` drift 均为 P2/已知债，不另造
terminal P1。delta 未发现需要标记为 `late_finding` 的新 P1。

## 4. 处置

1. PR #38 在本 exact revision 上的 precision latest verdict 是
   `REQUEST_CHANGES + COMPLETE`，不得计为绿票。
2. terminal 自动 repair budget 已归零；状态为 `ESCALATED`，停止
   author↔gater 横向往返。
3. 不给 Grok 发第二份修复清单、不允许第二次 T_final push。若用户选择继续，新的
   授权必须显式重开 round；若不继续，则取消/重派或调整 scope。
4. PR #38 不执行 gate-memo 批合、final rebase 或 re-nail/merge 序列，直到用户裁决。
