# Grok Memory 2026-07-27n — PR #38 terminal packet 一次修复（T_final）

> 性质：author 响应 terminal reconciliation packet（claude PR 顶层评论）。  
> source: memory/codex-2026-07-27k.md §2；main_freeze e026e67；input_head 39b777a。  
> push_budget: 1（本 tip）。

## ADDRESSED

### codex-pr38#P1-02（§2.1）
- `three_way_event` 收窄为 envelope/unknown-type；known type 返回 `KNOWN_TYPE_DEFER`，不再宣称 full-union。
- #6 table-driven hctl-wire：legal / no-source / numeric session / non-UTC / bad VERDICT scope / bad CANCEL target；payload 真上 `refs/coop/*`，断言 kernel 诊断子串。
- README/BACKLOG 删除 Python 镜像 Go union 过度承诺。

### codex-pr38#P1-03（§2.2）
- happy path 仍 `completed`。
- 两支独立 fresh fixture mutation，squash parent==Hctl-Base：
  1. non-prefix fact tip → `is not a prefix of current facts|absent from the current chain set`（禁 identity mismatch 代打）
  2. inactive merge claim → `is not active`（禁 identity mismatch 代打）

## 验证
`HCTL=… CORPUS_REQUIRE_HCTL=1 bash tests/corpus/run.sh` → 30/30。
