# Grok Memory 2026-07-27g — p1-kernel 对抗 gate verdict（PR #33）

> 性质：**required gate verdict**（assignment `p1-kernel` / gate id `adversarial` / seat `grok`）。  
> 对象：PR https://github.com/yesme/hctl/pull/33 · 分支 `work/codex/p1-kernel`。  
> 会话：Grok 4.5 / 对抗镜头。  
> 输入：PR 全文 + head 树 `internal/**`/`cmd/**` + `memory/codex-2026-07-27f.md` + DECISIONS D-31..D-44 + assignment `.hctl/assignments/p1-kernel.toml`。  
> 验证：`GOPROXY=off go test -mod=vendor ./...` PASS；`go test -race` store/derive PASS；本机 `go build` + `doctor`/`status`/`help` 冒烟。

---

## 0. Verdict（D-41 / D-40）

```yaml
gate_id: adversarial
gater_seat: grok
assignment: p1-kernel
decision: APPROVE
completeness: COMPLETE
threshold: P1   # review parameter only; kernel reads decision only (D-40)
scope:
  kind: full
revision:
  base: "85643c6d946f2042cb90d7e6ad84b75e44fd8585"
  head: "d91f38d93b9a1c59521b9b16df157f3cfffdbe7b"
report: memory/grok-2026-07-27g.md   # this memo; merge to main before any machine VERDICT
late_findings: []
```

**总判：APPROVE + COMPLETE。**  
P1 竖切在对抗面（CLAIM 双层 CAS、fencing、capacity、fail-closed 事实三分、merge receipt 双时点、gater≠author）上成立；作者四项 escalate（E1–E4）为诚实边界而非暗裁。无 P0/P1 阻断项。

**exact revision 核验**：
- `git rev-parse origin/work/codex/p1-kernel` = `d91f38d93b9a1c59521b9b16df157f3cfffdbe7b`
- `merge-base --is-ancestor 85643c6… d91f38d…` 成立（base 为 PR 声明 merge-base）

---

## 1. 对抗审查范围与方法

| 面 | 动作 |
|---|---|
| 传输/互斥 | `store.Publisher` lease push、Recover delivered/lost/recovery ref、同 tip 双 CAS |
| 身份 | JCS + obligation id 管线、ASCII token、dup-key 拒 |
| derive | stale/streak/escalated frozen、selectVerdict fencing、capacity、quorum、scopeCovers |
| 写动作 | claim/begin/verdict/merge 前置守卫、WriteGuard on incomplete facts |
| merge | coordinator-only、green+exact base/head、receipt body-file、post-verify |
| 配置法律 | gater≠author、required+proceed 非法、pattern 相交、enforcement |
| 运行 | unit/race、build、doctor/status 对真实 main 的红灯诚实性 |
| 作者 escalate | E1–E4 是否暗含错误默认真理 |

未做：真实 `gh pr merge` 打生产仓；跨机双 clone 人工对射（有 store 单测同构）；全 corpus 在本会话重跑（PR 自报 `HCTL=… run.sh` 30 PASS，抽查 JCS 与 store 单测交叉）。

---

## 2. 攻击结果（按严重度）

### P0 — 无

### P1 — 无阻断

审过且**不**升阻断的候选：

| 候选 | 结论 |
|---|---|
| 无 `hctl cancel` CLI 而 D-34 thaw 含 CANCEL | **不阻断**。D-43 命令表未派 cancel；CLOSED 事件可 parse/derive；thaw 仍可用新 assignment revision（人裁）或未来 CLI。作者 E4 已 escalate。 |
| `scopeCovers` 仅要求 fix+delta 零件 | **不阻断**。E3 已写清 schema 无 surface map；保守机械判据优于伪造全覆盖。 |
| seat 检测 branch 优先于 worktree | **降 P2**（见下）。单账号信任模型下非提权；holder 不匹配时写动作 fail-closed。 |

### P2 — 残留（不阻塞 APPROVE）

1. **Seat 检测冲突静默**（`identity.DetectSeat`）  
   当前：分支 pattern 命中即返回，**不**与 worktree 后缀交叉校验。在 `hctl-grok` checkout `work/codex/*` 时 doctor 报 `seat=codex`。  
   **建议（follow-up）**：worktree 后缀与 branch 席不一致时 **硬错误**，要求 `--seat`/`HCTL_SEAT`，禁止静默选边。

2. **CANCEL 无 CLI**  
   escalated 冻结后操作者路径窄。建议 P1.1 或 P2 补 `hctl cancel` 薄命令（仅写 CANCEL 事件 + authority 声明），与 D-34 对齐。

3. **Activation / bootstrap 审计债（E1/E2）**  
   本机 `status`/`doctor` 正确亮 `BOOTSTRAP_TRUST` + p1-corpus `UNRECORDED_MERGE`，**未**伪造 receipt——对抗赞许。合并 kernel 后仍须两步 pin+activation；不得同一 squash 自指 OID。

### P3 — 低

- `gitx` 无单测（逻辑薄、argv 安全可接受）。  
- `checkEnforcedHCTLChange` 依赖「有 required gate」+ 全 revision quorum，不单独证明「审过 .hctl 路径」——与 full-revision APPROVE 模型一致。  
- help 文案完整；trailer fail-closed 依赖 env——符合 D-23 精神。

---

## 3. 正面对抗结论（通过项）

| 护栏 | 证据 |
|---|---|
| 远端 lease 成功 = CLAIM 线性化点 | `store.Append`：local CAS 后 `pushLease`；失败 → pending，不返回 acquired |
| pending 恢复 | ancestry delivered；分叉先 `refs/hctl/recovery/` 再对齐 |
| 未知控制事实 fail-closed | `UNSUPPORTED_FACTS` / `WriteGuard`；写前 `ensurePublishingClean` |
| 永不自动 re-claim | 显式 `--reclaim`；active 无 flag → already-active |
| escalated frozen | claim 拒 re-claim；verdict 拒；进度不清红（classify + evaluate） |
| capacity=1 merge/author | claim 前扫描 + derive `validateCapacities` |
| merge coordinator-only | claim + merge 双检；loader 派非 coord 在 config 层 |
| gater≠author | `config` load：`gate gater is also the author` |
| strict base/head | merge 对 main/PR head；gate claim `revision_at_claim` |
| receipt 封闭 + body-file | `runGHMerge` 用 `--body-file`/`--match-head-commit`，无 shell 拼接 body |
| F3′ 精神 | pre-merge 钉 current config；历史 receipt replay 在 `merge_history`（作者 memo §1） |
| 自描述输出 | claim/begin/status 含 obligation id、holder、next |
| 诚实红灯 | 真实 main 上 UNRECORDED_MERGE / BOOTSTRAP_TRUST 可见 |

---

## 4. 对作者 escalate 的对抗立场

| ID | 立场 |
|---|---|
| **E1** 两步 activation / pin | **同意**。禁止同一 squash 自写 OID；后续 gated PR 写 `kernel=hctl@<K>` + `enforcement=active`。 |
| **E2** bootstrap 无 audit baseline | **同意** 不伪造 receipt。coordinator 须在 activation 前裁 cutover（baseline / repair / 推迟）。 |
| **E3** composite 覆盖 | **同意** 保守 `full` 或 fix+delta 零件；更强覆盖待 schema。 |
| **E4** cancel/helpers phase | **同意** D-44  defer；建议在 DECISIONS 显式 amends 一句消歧。 |

---

## 5. completeness

```
completeness: COMPLETE
```

本 full 范围内：**无保留发现**可阻止 APPROVE。  
P2 残留与 E1–E4 为 **follow-up / 排班边界**，不构成本 revision 的 REQUEST_CHANGES。

**给 merge coordinator（claude）**：本 gate 在 exact `{base,head}` 上绿灯。尚需 **spec-conformance（claude）** COMPLETE+APPROVE 后方可 D-37 人肉/日后机器 squash。合并后勿改 head 而不 re-gate。

---

## Process Notes

- 审查在 detached 跟踪分支 `work/codex/p1-kernel` @ `d91f38d` 进行；本 memo 自 `origin/main` 开 `memo/grok/p1-kernel-adversarial`。  
- 写围栏：仅本文件。  
- 微 PR 自开；bootstrap 期 memo 自合以使 gate 事实进 main。

## Commits 本次会话

- 本 memory（对抗 gate verdict）。
